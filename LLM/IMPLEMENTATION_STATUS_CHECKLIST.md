# Neva Implementation Status & Evidence

> This file records evidence, not design authority. Root `AGENTS.md` routes
> work; `03` owns gate definitions. Read the navigation table first, then the
> exact record and its input scope. An earlier pass is not a current build,
> browser, visual or release certificate.

## Current navigation

This table separates stable owners from recorded acceptance. It does not
re-run or revoke a previous scoped result. For a new completion claim, verify
that the evidence covers the changed inputs and the gate being claimed.

| Area / gate | Where to read the current contract or result | Evidence boundary |
|---|---|---|
| Architecture, state and migrations (P0/P2) | `01` §5–§7; `src/persistence/SaveSchema.ts`, `SaveMigrations.ts`; retained fixtures | Only `01` §6.1 owns migration history. A source/type inspection is not a save/reload test. |
| Gameplay/content (P3–P11/P13) | `02`; `src/content/` and `ContentRegistry`; `content:validate` | Registry validation establishes content consistency, not player comprehension, balance or continuous traversal. |
| Visual direction (P0.5/P0.75) | `04`; `tests/visual/reference/approved-baselines.json` | The registry records accepted bridge/farm/harbor/coast human scope. It does not approve later changed scenes or close render/strict/determinism gates. |
| Published asset state and technical certification | `generated/reports/asset_budget_report.json` and generated/public manifests; `BLENDER.md` §2/§6 | Published state is distinct from run-local rejected candidates. Read the current report; no copied asset count or debt list here. |
| Product loop (P12) | `03` §18 and `02` §19; retained product record below | A human quest-ending playthrough was recorded. Automated continuous save/reload and release browser coverage remain independent evidence. |
| HUD, world, physics and audio changes (P1/P14) | Their owning documents and source; retained focused records below | Each source/unit/browser/listening result applies only to its stated scenario. Current integrated visual/mix approval is a separate claim. |
| Performance/browser/release (P15/P16) | `03` §21/§22; §4 validation matrix; isolated production reports | DEV diagnostics, distribution size, software rendering, hardware timing and full-loop browser acceptance prove different things. A release claim needs the required matching-input records. |
| Guidance refactor | Current evidence below | Documentation/content consistency only; gameplay code, budgets, schema and published assets were not changed by this task. |

## How to record evidence

Keep new records in **Current evidence**, before the historical block. Update
the affected navigation row when a gate gains or loses relevant evidence;
retain superseded results below instead of rewriting their outcome.

A bounded record contains:

- Changed contract and scope, including save/migration impact when relevant.
- Command or actual human/browser action, result, and evidence artifact.
- Input/build identity for expensive or concurrent runs; note observed drift.
- What it proves and what remains outside that run.

Use explicit evidence labels: **source checked**, **automated pass/fail**,
**browser observed**, **human accepted**, or **not run**. Include the date and
scope of an observed result, not a blanket file-wide freshness date. Keep gate
state separate from implementation progress. Never infer a full pass from a
focused retry or a historical green row.

## Current evidence

### 2026-09-16 — Authored farm kitchen (v45), Sunreach translation guards, capacity/mooring hardening

- **Changed contract (`01` §6.1 v45; save-sensitive):** the farm kitchen is now authored — `struct.kitchen` (`stationType: "kitchen"`) in the yard pocket southeast of the farmhouse at local `(11.0, -4.6)` facing the house door, registered in `FarmLayout`, `WORLD_STATION_DEFINITIONS`, the starter farm `structureIds`, `RuntimeAssetOwners`, `WorldScene`, `layoutEdit`, and `ProcessingStationApproach`. Its building is a new catalog asset (`building_farm_kitchen_a`, `village_building` generator `kitchen` variant: plank walls, lean-to roof, masonry hearth bulge with oven mouth, serving counter, firewood niche, chimney). The three meal recipes (`cook_harvest_bowl/fish_stew/orchard_tart`) move off the workbench to the kitchen. `migrateKitchen45` adds the structure/placement to older saves and moves any in-flight meal job to the kitchen so `station.type === recipe.stationType` keeps validating. `CURRENT_SCHEMA_VERSION = 45`, `world.layoutRevision = 20`.
- **Fixed stale guards:** `terrainLayoutMigration` / `terrainLayout12Migration` now expect the single v42 ocean translation (`x >= 300 → +800`) for structures/boats instead of asserting no move; the Sunreach vessel case seeds the legacy pre-offset pose. Farm capacity rejections use the farm's authored `label` (`WorldFarmDefinition.label`) instead of a hardcoded Commons string. `migrateOceanLayout20` re-docks incoherent `isDocked/dockedMarketId` boats instead of letting the strict v42 validator read them corrupt, and backfills a missing `dockedMarketId` to null.
- **Evidence:** `tsc --noEmit` clean; scoped ESLint clean; `npm run content:validate` passes (48 items, 30 recipes, 3 markets). New `tests/simulation/kitchenMigration.test.ts` (authored anchor, kitchen-only meals, v44→v45 move, idempotence); updated `workEconomy`, `persistence` (kitchen anchors, `dockedMarketId` backfill tolerance), `farmingPerfection` (kitchen anchor list), `processingStationApproach` (kitchen list/front/corridor), `migrationPreservation` (kitchen placement tolerance), `starterIslandTraversal` (kitchen collision proxy). Sweeps green: `kitchenMigration`, `workEconomy`, `terrainLayoutMigration`, `terrainLayout12Migration`, `persistence`, `processingStationApproach`, `farmingPerfection`, `simulationLoop`, `gameplayFixes`, `laborMechanic`, `limitsProbe`, `contentReachability`, all four `doc*` suites.
- **Not claimed:** no Art Yard or browser visual inspection of the kitchen prop/approach; no human playtest of kitchen cooking flow. `starterIslandTraversal` farm-headwater downhill loses contact at `(-102,-84)` — far from the kitchen and outside its collision, failing identically with or without this change (in-flight layout-20 terrain work, recorded as pre-existing). Full-suite triage and preservation-baseline refresh remain open.

### 2026-09-16 — Reject-safe knowledge journal (v44) and title-screen boot crash

- **Fixed crash:** `GameApp.renderUI` queried `this.sim` for the labor HUD before the `bootReady` guard, so the constructor's first title-screen render threw `Cannot read properties of undefined (reading 'query')`. The query now sits below the guard.
- **Changed contract (`01` §6.1 v44; save-sensitive):** a pre-v9 build granted `quest.rewards.unlocksFeature` into both the feature list and `journal.unlockedKnowledge`, so veteran saves carry capability ids (`boat.player_rowboat`, `feature.expedition_planner`) where the journal expects knowledge ids. `migrateKnowledgeJournal44` keeps only registry-owned knowledge ids; `quests.unlockedFeatureIds` already owns the unlock, so no capability is lost. `CURRENT_SCHEMA_VERSION = 44`, `world.layoutRevision = 20`; the strict `journal.unlockedKnowledge` registry check stays.
- **Runner hardening:** `migrateSaveData` also reconciles a slot whose stored `world.layoutRevision` trails the shipping revision — a development build that tagged the head schema before the matching layout migration landed — instead of reporting it corrupt.
- **Evidence:** `tsc --noEmit` clean; new `tests/simulation/knowledgeJournalMigration.test.ts` (drop, capability retention, envelope immutability, idempotence); `tests/simulation/oceanExpansion.test.ts` 10/10 including a trailing-layout repair case; `tests/unit/docArchitectureClaims.test.ts` 5/5.
- **Not claimed:** no Art Yard or browser visual inspection.

### 2026-09-15 — Work Capacity becomes an earned daily labor budget (v43)

- **Changed contract (`01` §6.1, `02` §14; save-sensitive):** Work Capacity is no longer restored by a per-game-hour rate. The pool is restored by a night's rest (`WORK_REST_FRACTION` plus `WORK_REST_BASELINE_FRACTION`), by eating a crafted provision (`ItemDefinition.consumable`, `WORK_MEAL_DAILY_LIMIT`), by a labor-shift minigame at an authored chore station (`LABOR_STATIONS`), by skill rebates on a perfect basic catch or a landed sport fish, and by a slow real-time idle trickle (`WORK_PASSIVE_REGEN_AMOUNT` every `WORK_PASSIVE_REGEN_INTERVAL_SECONDS` real seconds, ceiling-clamped and cap-exempt). Meals, labor and skill share one `WORK_DAILY_EARN_CAP`; the ceiling rises to `WORK_CAPACITY_MAXIMUM = 500` with rest, meals, labor, skill and the trickle scaled to the wider pool. `ProgressionDomain` remains the only writer. `WorkCapacityState` gains optional `earnedToday`/`earningsDay`/`mealsToday`/`laborUsedToday`/`passiveRegenSeconds`; the new `stationType` `"kitchen"` is supported but not yet anchor-authored, so meals are cooked at the starter workbench.
- **Save impact: yes — schema 43.** `migrateWorkCapacity43` rescales the legacy 1,000-point pool to 500 preserving fullness and backfills the daily tallies. `CURRENT_SCHEMA_VERSION = 43`; `world.layoutRevision = 20` unchanged from the in-flight ocean revision. Ledger rows for v42 and v43 were added to `01` §6.1. Fixtures: the retained `tests/fixtures/save_v41_layout19.json` through the full chain.
- **Automated pass:** `npx tsc --noEmit` clean; scoped ESLint clean on all changed source and test files; `npm run content:validate` passes (48 items, 30 recipes, 3 markets). `tests/simulation` passes 666/671 — the 5 failures are the pre-existing layout-20 ocean-translation expectations (`terrainLayoutMigration`, `terrainLayout12Migration`, `sunreachShoreMigration` x/z `+800` drift), not Work. New/updated coverage: `workEconomy.test.ts` (rest floor, daily cap, meal limit, labor proximity/sweet-spot/once-per-day, meal recipe advertisement), `workCapacityMigration.test.ts` (rescale, backfill, immutability, idempotence), and rewritten `laborMechanic`, `limitsProbe`, `persistence`, `fishingCargoFixes`, `equipmentCraftingSystem`, `p12VerticalSlice`, `farmingPerfection`, and migration-preservation Work assertions.
- **Not claimed:** no Art Yard or browser inspection of the Work meter, meal flow or labor widget; no human playtest of the 25–35 actions/day pacing target; the `tool/world` preservation baseline (`neva-layout19-working-preservation.json`) is untouched, so `starterIslandPreservation` remains red from the in-flight layout-20 work.
- **Follow-up hardening:** `hasWorkRoom` now requires room for the full grant, so a meal or a labor shift is never consumed for a partial restore; a labor shift abandons itself if the player steps out of reach or opens a fishing encounter, and only marks a station used once Work is credited; `SatchelPresentation` defaults `edible: false` so an unaugmented item DTO never advertises a meal; `ContentRegistry.validateProgressionAndEquipment` rejects a malformed `consumable`; the dead passive-regen function and its two zero-rate constants were removed; and the obsolete `WorkCostQuote.readyAtMinute` / `work.recharging` contracts and their dead CSS were removed. The idle trickle is measured in real unpaused seconds, persists its accumulator, and never accrues offline. `workEconomy` covers the full-room gating and the walk-away abandons; `laborMechanic` covers the trickle rate, ceiling clip and paused/offline behaviour.

### 2026-09-15 — Compact market-square village, denser dwellings and realistic NPC walking

- **Changed contract (`01` §2/§10, `WorldLayout`/`WorldAnchors`/`WorldEnvironmentLayout`, presentation):** the village court is recentred on `market.village` at `(39.7, -65.9)` — the plaza plateau shrank from `36×32 m` to `22×20 m`, the `village-market` junction from a 9.2 m disk to 6.0 m (blend 2.8→1.6), and the four converging routes narrowed (`farm-village` 3.8→3.2, `village-harbor` 4.2→3.2, `village-homestead` 3.1→2.6, `village-lighthouse` 3.2→2.6). Two `house_cottage_a` dwellings (west/south pads) and a second `house_farmhouse_a` (southwest approach pad) join the relocated farmhouse and market hall on the square ring, with doors computed to face the stall. The board, well, oven, benches, kitchen bed, wagon, hay, chickens and lamps moved to the compact court; ambient townsfolk grew from 3 to 6 routes with eased multi-waypoint loops. Named NPC beats gained a fourth waypoint, per-NPC cruise speeds matched to each model's walk reference, and eased legs; the per-frame turn rate is clamped so a corner cannot spike the animator into a whole-body turn. The mill's working face now opens on the new market instead of the removed plaza.
- **Save/layout impact: none at schema level, layout-level only.** Layout 19 is uncommitted and unshipped, so its terrain/anchor changes are amended in place; no new schema version or migration was added and no save fixture changed. `migrateTerrainLayout19` still owns the v41 Commons normalization.
- **Preservation:** `vite-node tools/world/terrain-preservation.ts --capture-working` froze `tools/world/neva-layout19-working-preservation.json`; `starterIslandPreservation.test.ts` now compares the live world against that revision baseline (layout-10 fixture retained as history) and passes 12 tests. `node tools/world/acceptance.mjs --refresh-preservation --skip-checks` captured `tools/world/neva-layout19-preservation.json` (layoutRevision 19, sampleCount 4544, 64 seed placement hashes).
- **Automated pass:** `tsc --noEmit` clean; scoped ESLint clean; `npx vitest run` over the 14 affected files passes **140 tests** (`worldLayout` 35, `npcStationBeat` 8, `ambientTownsfolk` 6, `practicalLights` 5, `villageBulletinPlacement` 4, `rainShelter` 1, `starterIslandPreservation` 12, `worldPreservationReference` 2, `processingStationApproach` 7, `roadEnvironment` 5, `roadGeometry` 10, `questWayfinding` 17, `farmingPerfection` 27, `p12VerticalSlice` 1).
- **Not claimed:** no Art Yard or browser inspection of the reshaped square, so no human visual approval and no integrated frame/memory result. A full `npx vitest run` after this task's fixes reports **2,138 passing / 38 failing across 9 files**. The failures are unfinished work already in the worktree, not regressions from this task: the Commons farm-bounds change (`widthMeters`/`depthMeters` 7→21/12) that the migration `preserveResources` suites still compare (`terrainLayoutMigration` 12, `terrainLayout12Migration` 7, `sunreachShoreMigration` 9, `harborCoastMigration` 1, `worldDressingMigration` 2), the region-Commons rename escaping `&` in the HUD (`hud_m1` 1), the WIP market move not yet reflected in `irrigationWell` (1) and market commodity content (`landscapeOverhaulMigration` 4), plus one unrelated flaky skiff-helm grip case in `characterEquipmentSource` (1) that passed in the preceding full run. They were already red before this change and are not closed here.

### 2026-09-14 — Story flow, heralds and progression blockers (gameplay/lore pass)

- **Found by play, not by reading:** a real `Simulation`, imported from the running Vite dev server into the in-app browser (persistence off, nothing written to disk), was driven from a fresh save through Act 10 with real commands (plant, water, process, fish, fight, sail, dock, sell, talk). It found: Act 7's golden bream impossible on the starter Willow rod (20 of 20 casts at Sunreach were sardines); Neva's trench offshore point never spawning (42 of 42 offshore schools over 8 in-game days at the outer grounds), so Act 9's marker pointed at empty water; Act 7's speaker Tomas reachable only after the crossing his intro explained; `NpcTalked` crediting every track's talk step, which completed the Family Ledger's seed-pouch step unheard and let side-errand intros hijack the Act 10 round; Act 9 and the freight track waiting on the contract board's dice; and copy that contradicted mechanics (tutorial wheat "takes a morning", the village stall "near the farm edge", absolute seasons, a "second" bream one catch satisfied).
- **Changed contract (`02` §0.1, §7, §15, §17; `01` §2.1, §14):** one conversation per talk (`ConversationSegment`s: close → talk step → chained next ask → unprompted asks, main track first); talk steps credited only for the thread spoken; turn-in costs not taken in the breath that asked; quest `herald` and objective `dialogue` content (Act 7, Act 9, Act 10 round, The Long Way Round); `QuestStepAhead` feedback; beckon barks and a post-conversation bark hold; per-spawn-point school occupancy with per-habitat pressure; Sunreach's coast school on the reef edge; fishing-ground anchors derived from spawn points and fish-step markers on the nearest matching live school; acquisition requirement lines and the journal's `brief`; quest-aware contract refill, `contract.pass` and `tag:cross-channel`; Golden Sea Bream on the Willow rod; Heavy Sport tackle at Sunreach Cove; rod refusals that name the rod and its stalls; three family-throughline journal entries with an idempotent knowledge-only reconcile. **Save impact: none** — no schema, migration or `layoutRevision` change; every addition derives from existing saved state or is content/presentation.
- **Automated pass:** direct `tsc --noEmit` and scoped ESLint clean; `npm run content:validate` passed with unchanged counts; the full `npx vitest run` passed 250 files / 2,159 tests, including the new `tests/simulation/storyFlow.test.ts` (19: conversation shape, seed-pouch ask before reward, legacy unheard asks, report-backs, costs, Act 10 farewells, heralds, beckons, Willow-rod bream from the skiff at the reef, trench spawning, reef marker, rod advice, quest-aware board, pass refusal, cross-channel tag, knowledge reconcile, work-ahead notice) and the updated `questDomain`, `questEarlyActionCredits`, `questExpansion`, `questTracks`, `npcSchedules` and modal render tests. Later dialogue-layout edits and `tests/unit/storyFlowPresentation.test.ts` (4) passed in their focused runs.
- **Browser observed (DEV `?debug&debugStart=farm`, in-app browser):** Elspeth's beckon bark; one conversation carrying welcome → reward → planting ask; the sow completion beat with its heading and reward plate. The 250px responsive dialogue cap pushed text under a scrollbar once beats gained headings, so the cap rose to `min(380px, 60vh)` and page dots moved into the footer; re-measured body height equalled its content. No human visual approval is claimed.
- **Not claimed:** the P12 Chrome route result is recorded separately below when it completes; the 7,500 Fishing XP skiff gap was kept by decision and is now explained in-world and on the tracker, not re-balanced; the dialogue card's warm-material re-skin was deferred by the user; no human playtest of comprehension or pacing.

### 2026-09-14 — Star points and walking contact continuity

- **Source checked / automated pass:** `AtmosphereSky` composites pixel-filtered directional star points in its existing display pass after cloud reconstruction; the existing target alpha carries cloud/moon visibility. `HumanoidFootSupportSolver` blends the complete solved leg/independent foot by contact weight, and `HumanoidAnimator` eases isolated loop-zero contacts. `VisualRenderConfig` owns star and contact-fade tuning. Save impact: no; migration: none; no character GLB or source clip change.
- **Automated pass:** 54 focused tests across `animationController`, `humanoidFootSupportSolver`, `humanoidRuntime`, `playerPresentationBuffer` and `atmosphereSky`; `tsc --noEmit`, scoped ESLint, scoped diff check and a direct Vite production build passed. New tests cover partial/zero contact correction and walk boundary continuity; existing slope, reduced-motion, boat and mount support tests remain green.
- **Browser observed:** isolated Chrome production gameplay-camera keyboard/mouse walks at High/night and Low/day with reduced motion covered about 15 metres each. All 884 sampled frames remained grounded and retained consistent character mesh submissions; both game runs had no page/console errors. Star studies covered all tiers, DPR 2, additional orientations, day and storm; Art Yard runtime walk/run captures were inspected. Auxiliary study/Art Yard each logged one HTTP 404 resource message, with no page/shader exception; these are not clean-network certificates.
- **Evidence boundary:** `output/stars-walk-fix/REVIEW.md` links the full supplied-video frame review, screenshots, motion recording, test log and source/build identities. Scoped source hashes remained unchanged at final comparison. Recording overhead, unrelated shared-worktree edits, broader world/performance acceptance, human visual approval and Safari (left pending by the user) remain separate.


### Meadow carpet, shared meadow colour and grass accents — 2026-09-13

- **Changed contract:**
  - The connected short-grass carpet is the renderer-owned `MeadowField` (`src/render/vegetation/`). It replaces the `foliage_grass_a/b/c` scatter. The terrain and the blades share `MeadowColorField`, which applies the `SeasonalTint` transform once.
  - `GroundCoverRenderer` bends rooted-cover normals toward the terrain normal.
  - The `grass_clump` family has slender blades, seed heads on tall tiles, and the opt-in `foliage` surface mode in `tools/blender/common/geometry.py`. `sunflower_stand` reuses the crop sunflower builders.
  - The Art Yard previews the carpet with `?ground=meadow`.
  - **Save impact:** none. No schema, migration or `layoutRevision` changed, and every other ground-cover category keeps its independent candidate stream and placements.
- **Automated pass:**
  - Direct `npx tsc --noEmit` was clean for this change. Unrelated `src/ui/GameUI.tsx` errors from concurrent work were later cleared, and the production `tsc` inside the budget build passed. Targeted `npx eslint` was clean.
  - The full `npx vitest run` passed 248 files and 2129 tests. That includes the new `meadowField.test.ts` (12) and `meadowFieldSource.test.ts` (7), plus updated world-layout, road-environment, ground-cover, terrain-material and quality-transition suites.
  - Catalog-dependent suites were re-run after the catalog edit: 42 passed. `art:codegen:check` was current.
- **Asset publication:**
  - `npm run art:generate` validated and published `foliage_meadow_tall_a/b`, `foliage_grass_a/b/c` and `foliage_sunflower_a`.
  - `art:test-builders` passed, and `art:determinism` passed for the five `grass_clump` assets.
  - A concurrent session regenerated all 17 procedural creature GLBs after the `geometry.py` change and found them byte-identical to their published files.
- **Automated fail, pre-existing gate (`npm run test:budget`):**
  - Baseline `output/render-budget-Pa5HHy`: 297 draws and 1,073,173 triangles.
  - After `output/render-budget-INUipb`: 293 draws and 1,103,627 triangles. `meadow_field` accounts for 330,624; instanced ground cover fell from 350,984 to 38,156.
  - The 220 preferred-draw limit was already failing before this change.
  - The GPU timer moved from p50 12.5 ms to 64 ms, but the machine had switched to battery power between runs. Same-build production A/Bs moved neither GPU p50 nor FPS when the carpet was hidden, the terrain meadow layer was stripped, or the skinned meshes were hidden. No performance claim is made until a matched-power rerun.
- **Browser observed:**
  - A DEV `debugStart=farm` session (persistence disabled) and the Art Yard `?ground=meadow` views for the carpet, `foliage_meadow_tall_a` and `foliage_sunflower_a`.
  - The `farm-mountains` view at `artQuality=low` and `artQuality=medium` rendered without page or shader errors. Draw ranges followed each tier's density prefix, and blade shadow receiving was off on Low and on for Medium.
  - Fixed review captures only; this is not continuous gameplay or human acceptance.
- **Automated fail, expected (`playwright.visual.config.ts` against the running dev server):** all 4 gold views differ, by 26–31% of pixels. The approved baselines also predate the current HUD and tree set. Baselines were not updated.
- **Open:** human game review of the new meadow, a matched-power production performance run, and a visual baseline refresh after approval.

### Video-led atmosphere and landscape foundations — 2026-09-12

- **Production performance measured, target still open — 2026-09-12:** `output/graphics-overhaul/production-framed.json` completed 12 normal-camera cases across High/Medium/Low and 24 warmed 20-second timing windows without page/console errors, using asserted mouse-orbit headings on Apple M4 Metal Chrome at 1440 × 900 / DPR 1. High farm/offshore were near 60 FPS; harbor measured 54–60 and storm 45–53. Medium/Low were mostly near 60 FPS, but harbor draw counts exceed every tier and Medium/Low farm triangles exceed their hard envelopes. The isolated draw diagnostic confirms multi-draw batching is active and attributes substantial cost to farm cover and harbor shadow submissions. `output/graphics-overhaul/PERFORMANCE.md` links raw timing, loading, memory and exact build/model evidence. These are stationary windows; the offshore input probe did not move the boat. The build predates later concurrent source changes. Safari remains pending by user choice; this is not a full performance or sailing pass.
- **Road-cap repair, automated pass — 2026-09-12:** the normal reef camera exposed a triangular hole between each rounded cap's offset fan center and its diameter. `RoadGeometry` now closes that face in the shared render/collision mesh. The new free-end coverage regression failed on the old farm-work-zone cap; all 10 road-geometry tests pass after repair. A retained 3,150-point before/after support probe measured only added support, up to 0.060 m. The pending layout-17 migration now also re-seats Neva cap actors vertically, preserving X/Z, mounted attachment and resources; 10 migration tests and three road-index tests pass. Direct TypeScript, scoped ESLint and the compiled build pass. The compiled High/Low normal-camera views remove the triangular hole. Targeted real-keyboard crossings at the reef and harbor reached their world-coordinate targets with every recorded sample grounded and no browser errors (`road-cap-runtime.json`, `road-cap-walk-runtime.json`). Evidence: `output/graphics-overhaul/road-cap-before.log`, `road-cap-checks.log`, `road-cap-support-delta.json`, `road-cap-migration.log`, `road-cap-typecheck.log`, `road-cap-lint.log`, `road-cap-build.log`. The earlier frozen route run predates this cap repair.
- **Shore material correction observed — 2026-09-12:** flat-water and flat-bed controls removed the residual fringe, while no-refraction, no-capture, unlit-bed and flat-normal controls retained it. `WorldLayout`'s binary sand/seabed weights were the cause; its derived surface field now blends across the coast and extends the shallow-bed color onto the blend's dry side without changing marine gameplay membership, heights or collision. The new continuity regression failed before the correction, then all three shore tests and 30 surface/Sunreach/batching/footstep tests passed. Direct TypeScript, scoped ESLint and the production build passed. `shore-color-final-runtime.json` fingerprints three error-free High/Low/final normal-camera views; the repeating color teeth are removed in the inspected views. Human approval remains open. That view exposed the road-end hole repaired in the later entry above. The frozen world run predates this material-only correction; its captures cannot represent the final color blend. Evidence: `shore-color-before.log`, `shore-color-checks.log`, `shore-color-checks-retry.log`, `shore-color-contracts.log`, `shore-color-typecheck.log`, `shore-color-lint.log`, `shore-flat-water-controls.json`, `shore-optics-controls.json`, `shore-geometry-controls.json`.
- **Implemented locally, shore and distant-water correction:** `waterShadingGlsl` now filters wave variation around the canonical headwater grade instead of flattening the stream toward horizontal before Fresnel. Sunreach's bounded continuous shore profile removes the land/seabed step while retaining the closed coast and offshore/inland anchors. The immediate toe spans a terrain-cell diagonal; rendered terrain, indexed bed and collision share the same shape. Schema 39 / layout 17 and its preservation boundary are owned by `01` §6.1.
- **Automated pass, shore scope — 2026-09-12:** the final shore geometry checks passed (2 tests), including all 21 coast segments and retained working anchors; the final migration suite passed (8 tests), including historical layout validation, mounted recovery, basic/sport fishing, resource preservation, repeat load and failed-primary backup recovery. A separate water/persistence/composition/documentation batch passed 75 tests in six files. Direct TypeScript, scoped ESLint and the compiled builds passed. The initial migration batch's three failures are retained; the mount recovery predicate was corrected to use canonical mount validity, and the test habitat/failure injection were corrected without weakening validation. See `output/graphics-overhaul/shore-migration-final.log`, `shore-linear-toe-checks.log`, `shore-contract-checks.log`, `shore-final-typecheck.log`, and `shore-final-lint.log`.
- **Browser observed, shore and saved-game scope:** `continuous-shore-runtime.json` records High/Low farm and reef views without errors. Its headwater movement attempt was staged inside water and produced zero movement, so it is not a route pass. The final `shore-linear-toe-runtime.json` records High/Low reef views and corrected normal-camera cove/spring-approach walks of about 3.96 m / 4.05 m, grounded at both ends with no page/console errors. `shore-save-browser.json` uses an isolated Chrome profile and the retained synthetic v38 fixture with a planted terrace: real Continue, movement, Escape → Save now and reload → Continue preserved the crop, resources and saved player position through v39. That geometry-stage capture reduced the large teeth but retained a smaller color fringe. Its controls (`linear-toe-controls.json`) did not isolate depth clipping or texel alignment; the later material correction above owns the resolution and current views.
- **World acceptance boundary, latest:** the layout-17 preservation reference was captured with a stable input digest (`layout17-preservation-final-refresh.log`). The first normal full-world run stopped at transient duplicate catalog palette tokens (`layout17-world-acceptance.log`); the catalog recheck passed. Its full retry passed static checks, both composition validators and the build, then failed input drift before browser testing (`layout17-world-acceptance-retry.log`). A byte-verified frozen checkout was captured with stable source inputs (`output/graphics-overhaul/frozen-checkout-GCSenb/frozen-inputs.json`), but its full run stopped at the concurrently updated atlas check, which reported stale packed WebP pages (`layout17-world-acceptance-frozen.log`). The frozen direct typecheck passed. The subsequent route run explicitly skipped that prerequisite and completed 36 High scene captures plus four Low/Medium captures without page/network errors, then failed at the village-market anchor (`layout17-frozen-routes-retry.log`). A local reproduction opened the real market from the stalled pose and walked onward toward the harbor (`village-arrival-runtime.json`); the harness now verifies the trade interaction instead of requiring the player inside the solid counter. A full rerun of the corrected harness remains pending. The separate Safari driver rejected session creation because remote automation is disabled; the user explicitly chose to leave Safari verification pending. Chrome production performance, full route acceptance and human visual review remain open.
- **Implemented locally, Sunreach surface integration:** the shared terrain material now derives a dry palette weight from canonical Sunreach drainage and uses protected slope exposure for pale stone. Ochre ground and olive/sage variation retain the seasonal wash and farm/marine surface boundaries. A single normalized byte per vertex survives spatial batching; heights, canonical surface weights, route geometry and saves are unchanged. Terrain explicitly opts out of the palette-object rain response because it already owns mixed soil/shore wetness; cloud shade and haze remain composed.
- **Sunreach verification — 2026-09-12:** 13 material/surface-field/rain tests passed; direct TypeScript, scoped ESLint and direct Vite builds passed. The first 54-test world/Sunreach/batching batch reported passing assertions but failed with an unhandled Vitest `onTaskUpdate` worker timeout (`sunreach-world-checks.log`). Isolated retries then passed 35 world-layout tests and 19 Sunreach/batching tests (`sunreach-world-retry-layout.log`, `sunreach-world-retry-island.log`). The failed batch remains retained.
- **Browser observed, Sunreach surface scope:** `sunreach-surface-runtime.json` fingerprints six error-free fixed views across cove, terraces, ridge, reef and the Neva bridge. `sunreach-ridge-runtime.json` separately checks the refined dry-slope exposure at High/Low and the protected terraces. GPU geometry inspection records Neva's zero dry-climate field and the shared terrain program without the object-rain patch. A normal High camera and short real keyboard walk at the terraces preserved grounded state; the subsequent farm load lost its browser context, so `sunreach-gameplay-runtime.json` remains a failed run with one completed view. These are visual/interaction probes, not human acceptance or the full performance/world gate. This pre-correction capture records the original jagged Sunreach waterline; the later shore entry above owns its current evidence.
- **Reference reviewed:** the complete 67.8-second attachment timeline was inspected as a two-frame-per-second visual sequence. `output/graphics-overhaul/video-review/provenance.json` records the original file hash, timing and nine contact sheets. The woodland exit, stream crossing, open meadow, distant ridges and gate approach inform this pass; weather beyond the clear reference is an extension of Neva’s existing seven types. This is visual frame-sequence evidence, not a claim of native playback or audio review.
- **Implemented locally:** `AtmosphereSky` and its shared `CloudShadows` field replace the old dome/cloud models; all quality tiers retain weather-shaped cloud masses. `WeatherPresentation` eases visible weather across sky, light, precipitation, cover and water. `AerialPerspective` adds height-sensitive haze in linear radiance, including the water path without fogging the captured background twice. `ExplorationFraming` reveals more horizon in open country, with manual orbit/zoom priority and reduced-motion suppression. This is foundation progress in the approved overhaul, not completion of all world art.
- **Browser observed, atmosphere stage:** the frozen `output/graphics-overhaul/atmosphere-final-dist` loaded bridge/clear/High, harbor/cloudy/Medium, open-sea/clear/High, farm/storm/High, coast/fog/Low and the real `C` Character & Gear preview with no page or console errors. `output/graphics-overhaul/final-atmosphere-runtime.json` records compiled-script hashes and diagnostics; images are under `output/playwright/atmosphere-final-*`. These are fixed review views and a UI input check, not continuous gameplay or human acceptance. Earlier three-tier cloudy/storm and night captures are in `aerial-runtime.json`; they predate the last haze-color tuning.
- **Implemented locally, landscape stage:** broader Neva uplands, dry Sunreach shoulders, and an above-water Sunreach landing shelf use the shared rendered/colliding terrain. Existing destinations, route IDs, coast membership, spring and sailing gates remain. Connected short-cover density and `LandscapeWind` reuse catalog instances; grass and canopy share traveling gusts, rotated instances retain the world wind heading, and ground-cover provenance softens woodland/grove response. No new model assets were generated in this stage.
- **Save impact yes:** schema 38 / layout 16; `01` §6.1 owns the migration contract. `save_v37_layout15.json` was retained from a complete valid new-game envelope before editing terrain. `output/graphics-overhaul/layout15-inputs.json` records the old terrain samples and source hashes. The migration preserves resources and re-grounds land actors on their own island. Existing old-version tests now distinguish their individual migration boundary from the complete current chain.
- **Automated pass, bounded inputs:** 24 tests across Neva landforms, Sunreach, the new migration and architecture documentation passed after the first landform edit. After the landing-shelf correction, 84 tests in six persistence/Sunreach suites passed, including durable-slot backup recovery, active fishing, mounted recovery, resource/input preservation and repeat-load stability. The wind/cover/cloud-material batch passed 18 tests in four files. Direct TypeScript and Vite builds passed for `landscape-v2-dist`; targeted ESLint passed on the new atmosphere, camera, terrain, migration and wind code. The shared checkout also contained concurrent physics/NPC work; a missing `MathUtils` import in that movement path was repaired to make the combined build runnable.
- **Performance evidence scope:** the earlier matched, warmed High farm comparison measured baseline GPU median 7.82 ms and atmosphere-foundation median 8.24 ms, with frame p95 16.7/16.8 ms on Chrome/Apple M4 Metal. `foundation-performance-diagnostic.json` includes build hashes. That 15-second diagnostic predates height haze and the denser landscape and does not certify current performance or satisfy the production gate.
- **Browser observed, combined landscape:** `integrated-review-runtime.json` records four normal third-person views with real mouse orbit at the farm, bridge, harbor and coast. Compiled scripts and the published model snapshot are fingerprinted; the separate **Improve all vegetation assets** task owns that model production. Each scene loaded without page/console errors. The farm includes a short real-keyboard movement and a stationary motion interval; these diagnostic relocations do not establish continuous route acceptance. Images are `output/playwright/overhaul-integrated-*`; the recording is `integrated-gameplay-review.webm`. The original seven-weather matrix in `review-runtime.json` also loaded without errors and was visually inspected, including dawn, daytime rain/fog and night storm. It predates the final cloud reconstruction and later asset publication.
- **Cloud correction observed:** a sky-only comparison reproduced horizontal integration bands and the fine noise from jitter alone. Fixed spatial offsets plus a compact linear-radiance reconstruction removed that pattern in the inspected Low/Medium/High views, without temporal history. `sky-probe-final.json` records those three error-free shader renders; the combined gameplay views exercise the resulting implementation. The final cloud batch passes 10 tests in three files; targeted ESLint and the direct Vite build pass.
- **Landscape correction observed:** the lighthouse outcrop’s narrow underside contact looked suspended from the approach. Mesh/terrain samples in `headland-grounding.jsonl` confirmed the point contact, and its authored placement is now seated deeper. The same transform reaches render and catalog collision; the layout-16 migration consumes it. All 35 `worldLayout` tests and five `landscapeOverhaulMigration` tests pass after this correction. The isolated `headland-review-dist` build passes and its normal-camera coast capture has no browser errors (`headland-review-runtime.json`, `output/playwright/overhaul-headland-coast.png`). This supersedes the earlier focused footprint retry for the world-layout suite.
- **Composition evidence, bounded:** both 64-seed composition validators and their repeat-seed checks passed in the earlier `layout16-composition-validation.log` run, but overall acceptance rejected the changing input digest while atmosphere and concurrent asset files were edited. The post-outcrop reference refresh (`layout16-seat-freeze.log`) was also rejected by the input-drift guard while the separate grass pass was editing the checkout; it did not publish a refreshed reference. The earlier layout-16 reference remains unchanged. Its terrain/route/landmark and causal X/Z hashes do not encode this authored outcrop burial; a stable current-input acceptance run is still required after the grass inputs settle. This is not a complete world-acceptance pass. Revision-15 preservation and approved visual baselines remain intact.
- **Current performance is not accepted:** the combined normal-camera farm diagnostic sampled 660 frames over 20 seconds, with frame p50 33.3 ms / p95 50 ms after Auto reached Low on Chrome/Apple M4 Metal. The host carried substantial concurrent load. The later live/frozen/live sky diagnostic was also variable and gives no reliable isolated cost estimate; neither run establishes the desktop target or the production minimum. Raw timings and build/model fingerprints are in `integrated-review-runtime.json` and `sky-cost-diagnostic.json`.
- **Implemented locally, water and rain surfaces:** High-tier shallow caustics derive bounded focusing/defocusing from the visible optical normal and modify captured bed radiance without reapplying haze. Solar direction remains independent of the moon/lightning reflection key. A controlled headwater comparison isolated the large white patches to the old polygon foam mask; broken, filtered downstream ribbons replace it. Palette wood, stone and foliage now share the terrain wetness envelope with distinct matte albedo/roughness responses; semantic token metadata survives material renaming, and fogless previews/interior presentation opt out. These are presentation changes with no additional save migration, draw or render target.
- **Water/surface verification — 2026-09-12:** the first water batch passed 19 tests in four files (normals, shader linkage, capture ownership and headwaters); the coupled rain/wind/atmosphere batch passed 23 tests in five files. Direct TypeScript, targeted ESLint and direct Vite builds passed. `water-caustics-probe.json` records six error-free GPU cases: flat-surface neutrality within Float32 tolerance, zero-depth neutrality, signed ripple focus, rotation stability, time response and bounded extremes. `rain-material-probe.json` records distinct wet/dry Standard output at unchanged draw count and byte-identical fogless preview images. The first overly strict caustic tolerance and its measured roundoff are retained in `water-caustics-probe-initial.json`; no shader failure was hidden.
- **Browser observed, water/surface scope:** `water-review-runtime.json` fingerprints six error-free fixed views across bridge, spring, harbor and Sunreach, including High/Medium/Low and night storm. `weather-surface-review-runtime.json` records the first ribbon revision and harbor heavy-rain-to-clear presentation; 77 compiled foliage/cover consumers share wetness 0.9, then 0.1218 while drying. The artifact named `harbor-high-dried` shows partial drying, not a fully dry endpoint. The final shorter/softer foam is checked separately in `rapids-final-runtime.json`; these fixed review cameras and shader probes do not establish continuous gameplay, human visual acceptance or production performance. Build/model fingerprints keep these snapshots separate from the other task's later grass publications.
- **Browser observed, final grass integration:** the fresh farm-only retry completed at High with the latest shortened grass, real mouse orbit and a 1.5-second keyboard walk (`sunreach-gameplay-farm-retry-runtime.json`). The player moved about 2.94 m and remained grounded; no page/console errors occurred. The normal camera shows fuller meadow coverage beside the player. This does not upgrade the earlier combined browser run to a pass. That snapshot exposed the distant headwater reflection defect; the later normal-camera and canonical-grade correction above supersedes it.
- **Open:** finish the remaining route/landscape composition, against the full accepted plan, renewed four-slice human gameplay-camera acceptance, and stable matching-input world/production-performance runs on the requested browsers. Terrain/map, save/interaction, weather and lower-tier requirements remain part of that full scope; the surface batches do not close them. Older snapshots showing sparse cover predate the separate task's latest grass work.

### Gameplay trust pass — story hand-in, grade honesty, post-story closure — 2026-09-11

- **Changed contract (P0.1):** `npcAnchorAt(npcId, clock, quests?)` now returns a quest speaker's role anchor while that speaker's active quest final objective is complete and was earned away from them (any type except `talk-npc`, which completes inside the conversation). The renderer, nearby targeting, guidance and turn-in share the one station, so a quest giver cannot be visible at a social stop while the tracker points elsewhere. Barnaby keeps his Village Market day stop and waits at the farmhouse workbench for the Act 2 hand-in his dialogue names. Pure function of clock and quest cursor; no saved state, migration or schema change.
- **Content/copy (P0.2/P0.3):** the Act 1 watering line now states that a finer grade earns Farming XP and journal standing instead of implying a price (the live first-harvest feedback already said "extra XP, not extra gold"); Maeve's idle line no longer names quality-less mackerel as a cargo-quality fish; and Barnaby, Silas, Maeve, Tomas and Ines each gained a post-`knowledge.open_horizons` recognition entry, so the story has a spoken coda instead of stopping at Act 10.
- **Critical path and ladder (P1.1/P1.2/P1.3):** Act 6's fish-facing quests now pay 300 and 350 Fishing XP so the Expert skiff threshold is earned through the loop rather than a bare repeat catch; the 7,500 gate itself is unchanged. Four existing tool/outfit recipes moved from Skilled to Master (`recipe.deck_boots`, `recipe.long_spout_can`) and Artisan (`recipe.oilskin_coat`, `recipe.balanced_sickle`) so the back half of the processing ladder opens real gear; the v36 compatibility fixture was not touched. A seventh Tides quest, **The Silver King** (`quest.tides_blue_marlin`), makes `fish.blue_marlin` the optional-track capstone Silas sends the player after, so the icon fish has a story beat instead of only a Records entry. The optional track stays off the main spine and carries no new schema.
- **World reacts (P2.3/P2.4):** all eight arrival discoveries (`knowledge.discovery.farm`, `.spring`, `.coast`, `.beach`, `.reef`, `.ridge`, `.overlook`, `.bluff`) now trigger an NPC recognition line; each is placed first in its NPC's list so later earned milestones keep priority under the "latest matching" selector. `contract.bulk_grain_order` and `contract.bulk_cove_greens` are now requested by Barnaby and Ines instead of anonymous institutions. Copy only; no save impact.
- **Automated pass:** `content:validate` passes (45 items, 10 crops, 15 fish species, 27 recipes, 3 markets, 2 boats, 5 rods, 6 NPCs, 45 quests, 28 contract templates). Scoped Vitest files pass: `npcSchedules` 5, `npcRecognition` 6, `questDomain` 13, `questEarlyActionCredits` 8, `p12VerticalSlice` 1, `questExpansion` 10, `questTracks` 15, `rankUnlocks` 5, `equipmentCraftingSystem` 18, `contentReachability` 8, `seasonalAvailability` 7, `workProgressionPass` 3, `gameplayFixes` 54, `economyBalance` 13, `sportFishingPacing` 7, `contractBoard` 5, `empirical_m4_modals` 7, and documentation enforcement (`docContentCounts`, `docArchitectureClaims`, `docTypeShapes`, `docTuningValues`) 18. Direct `tsc --noEmit` and `git diff --check` pass; full ESLint reports 0 errors / 168 pre-existing warnings.
- **Browser observed — harness navigation rewritten, advancing, not a pass (P2.5):** `walkTo` now detects a real stall against the live colliders, attempts two bounded sidesteps, and only then falls back to a counted, logged debug stage; `readDiagnostics` retries through HMR detaches. With the earlier repairs (crafting-modal station flow, mill selector disambiguation, stable crop-placement sampler, gate turn-in, market/data-verified gateway, 3 m bridge staging, debug-staged station faces) a run reached **15.3 minutes**, clearing planting, watering, harvest, compost, Acts 1–2 and both bridge crossings and much of the route with **one** debug stage (at the village market gateway, where a stall sat against authored collision). That run ended on a `readDiagnostics` page reload, not a route defect: the working tree was being edited concurrently and Vite issued `page reload src/app/GameApp.ts` at 13:49 during the run. The whole-tree typecheck is also currently red only on unused imports the concurrent writer left in `src/render/scene/WorldScene.ts`. The route still has not completed, but its remaining failures are now one counted fallback plus concurrent-edit interruption rather than an unending series of tolerance wedges. A clean completion run needs the tree to be quiet: a second attempt at 13:54 was reset to the title/loading screen by the writer's `WorldAnchors`/`WorldScene`/`GameApp` page reloads, so no continuous run is possible while they are active.
- **Boundary and concurrent tree:** another writer was editing `src/world/WorldLayout.ts`, `src/ui/MarketModal.tsx`, `src/simulation/presentation/WorldAudioPresentation.ts` and several world-layout tests during this pass, so the aggregate full-suite run was abandoned after it exceeded 25 minutes against the moving tree and **no aggregate suite result is claimed**; every count above is scoped to its named file, and the bridge stall is likewise not attributed to this pass. No human visual review. Save impact **no**; no migration.

### Crafting, equipment and Character loadout — 2026-09-09

- **Implemented:** station crafting, permanent equipment, wardrobe reservations, clothing-only Field/Sea presets and the Character & Gear screen now share simulation-owned state and atomic commands. Schema 37 migrates the prior processing shape without changing layout 15; retained fixtures cover the v36 compatibility contract. Pending jobs keep immutable recipe/result/Work/XP/duration snapshots, and validation permits an empty station while rejecting more than one active-or-complete job at the same station.
- **Audit corrections implemented:** the integer Work discount floor is computed from the actual rounded neutral cost, so gear cannot exceed the stated 25% actions-per-Work gain; this is deliberately not an XP-per-Work promise. Item-output space is checked on collection, not start; content validation rejects duplicate output stacks and item batches that cannot fit the canonical empty satchel, while permanent-equipment results reserve wardrobe capacity before inputs or Work commit. The simulation owns action commit timestamps and exactly-once economic mutation; animation markers only display those timestamps, and pre-commit transient actions are discarded on reload. Water, harvest and information-only inspection use action-specific acquisition/quote/command reach. Field Hat affects planting and fertilizing instead of competing with the 20% watering/harvest tool ceiling. Purchased-input processing may earn a modest bounded margin after Work, station time and market supply changes rather than being categorically treated as free arbitrage.
- **Progression and compatibility evidence:** the design owner contains representative mixed-recipe routes to the first two Processing gates, including required inputs and repeat counts; focused tests pin those routes so Linen cannot silently become the only viable ladder. The pre-feature recipe fixture compares every prior output, Work reward/cost, authored minimum-skill gate, effective progression gate, authored duration and new-game/steady-state effective duration across the v36-to-v37 transition. Equipment remains a convenient work-outfit system with safe clothing-only switching; tools and rods are intentionally excluded from presets.
- **Presentation implemented:** the world and the real-time Character preview share `CharacterEquipmentAssembler`; selected catalog-generated wearables, tools, rods and station props publish through the existing manifest and budget report. Player source actions include `craft_tailor`, `craft_tool` and `gear_check`. Equipment and crafting-material sheets publish through the existing UI atlas. The four event-driven craft/equip cues are wired through the audio manifest; presentation failure cannot authorize or block a simulation transaction.
- **Automated pass:** direct `npx tsc --noEmit` and targeted ESLint on the feature surfaces; the focused gameplay/persistence/UI/audio/asset batch passes **17 files / 243 tests**. After the final compatibility-fixture and output-validator strengthening, the affected documentation/content/equipment delta passes **8 files / 51 tests**; the final real-command Field Hat check passes within the equipment system's **16 / 16 tests**. `npm run content:validate`, `npm run audio:normalize:check`, `npm run ui:codegen:check`, `npm run ui:publish:check`, and `npm run ui:atlas -- --check` pass. Selected asset generation, validation, source determinism and atomic publication passed for the equipment/prop set and the augmented player source. These are scoped results, not a replacement for a current full-repository suite or release certification.
- **Browser observed:** `tests/e2e/equipmentCraftingCharacter.spec.ts` passes in isolated Chromium against the direct Vite server. It opens the actual Character & Gear screen with `C`, verifies six loadout slots, a loaded 3D preview, atlas-backed gear, local Try On versus saved Equip, and reachable wardrobe, comparison and outfit controls at 844×390; then it moves to the Workbench, starts Weave Linen Roll, observes the persisted job, advances it to complete and collects it with no page errors. Captures: `output/playwright/equipment-system/{character-screen,character-screen-short-landscape,workbench-crafting}.png`.
- **Open acceptance boundary:** save impact **yes** (schema 37; no layout revision). Human in-game visual review of fit, silhouettes, clipping and animation transitions, plus human listening/mix approval, remain open. The Chromium scenario does not establish Firefox/WebKit, controller/touch, full progression balance, long-session economy, production performance or release readiness.

### Player-feedback pass: tutorial unblocking, pacing, readability, world collision — 2026-09-08

- **Implemented (tutorial):** an opt-in early-action credit ledger on `QuestState`. Four Act 1/2 objectives declare `creditsEarlyActions`; banking and redemption share the live path's own gate predicate, banking happens only when no active track accepted the event, and redemption runs strictly after `stepProgress` is cleared on advance. This closes a genuine softlock: watering during the sow step discarded the event, and `FarmingDomain.water` refuses an already-wet crop, so the watering objective was unsatisfiable for roughly 230 game minutes. New games no longer start with `item.bait_worms`. Schema 35 adds the ledger and repairs stranded saves; schema 36 / layout 15 follows for collision.
- **Implemented (pacing):** `ONBOARDING_PACE` is the single owner of the opening tutorial's clock. Starter-garden wheat grows 5x and the tutorial compost run takes 12 in-game minutes. `effectiveRecipeDurationMinutes` is the sole owner of job duration and is called by both `ProcessingDomain` and the interaction prompt, so `recipe.durationMinutes` now has exactly one reader outside content. The recipe pace is additionally gated on no compost having run, because gating on the quest alone would have been a 30x throughput hole on a profitable recipe rather than a pacing choice.
- **Implemented (readability):** the crop placement preview is four footprint-sized corner brackets plus a seed marker instead of a filled disc; the prompt says "Plant one X" with quest-owned progress. `CropInspectionDto` gained simulation-owned `maturityProgress`, rendered as a meter on the targeted crop only. The satchel states capacity once, hides search and tidy behind one Organize disclosure, and collapses agronomy under Details. The dialogue typewriter is keyed on a page generation token, so a stale reveal can no longer bounce the footer label from "Next" back to "Show all".
- **Implemented (movement):** walk 1.6 → 2.0 and sprint 4.4 → 5.2 m/s, with `MOUNT_TUNING` raised to 2.3 / 5.8 / 8.4 to preserve the rule that riding beats walking at every tier. `fauna_donkey_a` and `char_player_a` were regenerated so mount clip reference speeds and durations still match; mounted playback stays exactly 1.0.
- **Implemented (world):** mature oak, maple, pine and apple trees carry a lower-trunk collider and large field and coastal boulders a main-body collider. The seeded-fill blanket ban on colliding assets became an allowlist keyed on catalog family. Three presentation-only townsfolk were added around the market, inn and harbor.
- **Automated pass:** the full suite is green — **221 files, 1934 tests**, direct `npx tsc --noEmit` and `npx vitest run` with no asset-sync pre-hook. New coverage: `questEarlyActionCredits`, `onboardingPace`, `onboardingCreditsMigration`, `treeRockCollisionMigration`, `ambientTownsfolk`, `dialogueTypewriter`, `locomotionPlaybackRate`, `mapSidebarContrast`. `npm run content:validate` and `npm run art:validate -- --all` (258 published assets) pass; `npm run art:determinism` passes for the donkey and for 58 vegetation/rock assets.
- **Measured, not asserted:** starter wheat matures in 60–90 real seconds and the tutorial compost in 30, against 7.5 and 15 real minutes before. The new tree and rock colliders block **0 of 1031** authored route samples and no station approach, door, market or dock boarding point; the one blocked route sample in the network is the pre-existing authored `rock_spire_a` ridge landmark on Sunreach. Map sidebar contrast was measured in the running app across 40 nodes × 4 lenses with **zero** failures against WCAG AA, so the palette was left alone and only the chart's overlay scrim was darkened.
- **Browser-observed:** the user's real save migrated 34 → 36 and loaded to playable on `localhost:3000`; the bracket placement preview, the simplified satchel and the darkened chart were confirmed on screen; standing beside a background villager returned `interactionTarget: null`, and four figures were visible around the market at night.
- **Performance, measured and over budget:** the new colliders take the seed-42891 static proxy set from 105 to 462 boxes (+357). A Node/Rapier harness walking a sprinting player through wooded ground for 4,000 fixed steps, five alternating rounds, puts the median cost at 475 us/step before and 532 us/step after — **+12%**, against the 10% ceiling this pass set itself; a single earlier run measured +13.6%. The samples are noisy (276-730 us within one arm), so treat the ratio as approximate and the direction as real. In absolute terms the added ~57 us is about 0.34% of a 60 Hz frame. This is a Node measurement, not the production browser build, and no startup-time or browser frame measurement was taken. The ratio budget is **not** met; whether ~0.3% of frame time is worth solid trees is a product call, and reducing it would mean colliding fewer trees.
- **Layout 15 reference captured:** `npm run world:acceptance -- --refresh-preservation` froze `tools/world/neva-layout15-preservation.json` (layoutRevision 15, sampleCount 4544, terrain/water, route and landmark hashes, per-seed composition placement hashes). That path runs the Neva preservation audit and both composition audits first and refuses to freeze unless inputs are stable and seed 42 is deterministic, so those passed as preconditions. Normal acceptance then built the production bundle successfully and completed **20 scene captures** across both islands at layout 15. It then failed, explicitly and on its own terms: `movement samples exceeded the 60-minute acceptance deadline`, thrown from `assertMovementDeadline` after an hour of route walking, having reached `overlook-approach 13/21` on the mountain trails. That is a real acceptance failure, not an environment crash — and since this pass added roughly 357 static colliders at a measured +12% per fixed step, it is a plausible contributor rather than something to wave away. It ran on a machine also carrying this pass's other jobs. The processing/equipment refactor has since settled, but this normal acceptance run has not been repeated idle; its failure remains current for that gate.
- **Not claimed:** no human visual approval and no release browser matrix. At this historical checkpoint the production render-budget server was blocked by type errors from the then in-flight refactor. Direct typecheck now passes in the 2026-09-09 record above, but that does not retroactively run or pass the render-budget gate. The run clip is driven at about 1.59x its authored cadence on flat ground and 1.82x downhill; that is a recorded cost of the speed increase, pinned by test, not an approved look.
- **Superseded concurrent-refactor note:** the processing/equipment work described here is now integrated and evidenced by the 2026-09-09 record above; the roughly thirty type errors were an in-flight observation and are not current. The green 221-file / 1,934-test run in this older record predates the crafting/equipment change, so it remains historical rather than current full-suite evidence.
- **Pre-existing blocker, not introduced here:** the P12 Chrome acceptance route still fails at the Act 2 turn-in. `tests/e2e/p12VerticalSlice.spec.ts` walks to each NPC's authored home anchor, but the uncommitted NPC schedule feature (`npc.schedule`, `npcAnchorAt`, untracked `src/simulation/presentation/NpcPresentation.ts`) moves Barnaby to the Village Market for the `day` phase, so the player arrives at an empty farmhouse workbench while the quest marker points across the river. Act 2's authored dialogue still says "see me by the farmhouse workbench". Resolving it means deciding whether the schedule or the quest text is right, which is that feature's call, not this pass's. Three heavy suites (`uiAtlasStress` x2, `startupBuildParity`) time out only under concurrent load and pass on an idle machine at 18.6 s and 39.5 s.


### Composition audit semantics repaired — 2026-09-08

- **Implemented:** `WorldCompositionAudit` checks the distinct habitat roles in `04` §11 instead of imposing a tree-heavy scalar ordering on village and harbor. The raw spacing ratios remain in diagnostics; the Neva 1.35 limit now applies to a one-sided Wilson lower bound so a sparse histogram spike does not count as supported repetition. This is a documented change to the evidence rule, not a lower numeric limit or a seed exception.
- **Automated pass:** both island composition audits run seeds 0–63. Neva's frozen placement hashes remain identical, district rhythm passes on every seed, and seed 11 retains its raw 1.488 ratio while its supported lower bound is 1.071. The density-contrast, openings, route/access clearance, isolate and determinism gates remain in force. Sixteen focused tests cover seed regressions, real synthetic lattices at both guarded spacings, a supported excess, damaged district roles and preservation/document contracts. Direct typecheck, targeted lint and production build pass.
- **Save/visual boundary:** no placements, materials, terrain, collision geometry or gameplay state changed, so no migration or reference refresh is needed. This supersedes the two composition failures recorded below. The unrestricted browser retry completed the seed-42 bridge capture after the sandbox initially blocked its preview port. The broader capture matrix was deliberately stopped during starter-farm capture because this change is audit-only; the resulting browser-close error records that interruption, not a completed acceptance pass. Artifacts are in `output/world-connectivity-2026-09-08/composition-repair/`; mechanical acceptance is not human visual approval.

### Current-layout reference and seamanship gates — 2026-09-08

- **Implemented:** the skiff purchase requires 7,500 Fishing XP, matching its Expert advertisement; Offshore tackle moves to that same rank so Act 9 does not add another proficiency grind. Prices, preceding-rod ownership and the optional Master rod remain unchanged. Existing ownership is preserved; no save shape or layout change and no migration.
- **Automated pass:** 72 tests across `rankUnlocks`, `questExpansion`, `gameplayFixes` and `workProgressionPass`, including atomic rejection at 7,499 XP, purchase at 7,500 XP, Act 9 objective progress and reload ownership. Eleven reference/document checks pass; direct typecheck, content validation and targeted lint pass.
- **Reference repaired:** explicit `world:acceptance -- --refresh-preservation` captured revision 14 with input identity and all 64 placement hashes. Normal acceptance selects the live revision and matches terrain/water, routes, landmarks, sample count and placements; missing/changed fields and unknown seed hashes are tested failures. Historical layout-9 data remains intact. Art Pipeline §13.3 owns maintenance semantics.
- **Remaining automated failure:** normal acceptance now reaches seed 9 district ordering and seed 11 reed periodicity (1.488 against 1.35), rather than failing the historical reference. These quality limits were not relaxed and scene generation remains blocked. This supersedes the reference blocker in the earlier continuation record, not its separate visual snapshot failures or human review boundary. Evidence: `output/world-connectivity-2026-09-08/reference-repair/`.

### World connectivity continuation — 2026-09-08

- **Implemented locally:** pooled reward feedback and crop/interaction response; presentation-only camera reward beats; first-arrival knowledge, Chronicle notices and interruptible vista framing; existing-catalog trail/village dressing, riparian density and bounded far cover; scenic boat orbits; skippable new-game opening, contextual hints and the Records Board endgame tracker. The preceding world-life work supplies shared NPC schedules, barks, regional audio, adaptive music, seasonal tint and window emission. No new quests or GLB assets were authored in this continuation.
- **Save impact:** layout 14 / schema 34; `01` §6.1 owns the ledger. The retained layout-13 fixture and migration tests cover obstructed player/mount poses, resource preservation, input immutability and repeat-load stability.
- **Automated pass:** the final sequential `npm run typecheck && npm test && npm run lint` completed successfully: 1,844 tests in 208 files; lint zero errors and 167 warnings. Startup parity and all world-layout checks passed in that run. Earlier phase runs encountered recorded timeout and in-progress migration/descriptor mismatches; the final run supersedes those failures for these inputs. Logs: `output/world-connectivity-2026-09-08/`. The repository also contains concurrent changes; this full-suite evidence applies to the working tree exercised, not an isolated patch.
- **Browser observed:** real harvest input produced item and proficiency feedback; proximity barks hid during dialogue; nighttime music/window emission and seasonal uniforms changed; trail discovery notices appeared without page errors. An isolated fresh save showed the opening and skipped to control with no simulation step during the sequence. Debug teleport/time checks do not establish continuous travel, full-arc playtesting, listening approval or performance certification.
- **Automated fail / review open:** `npm run world:acceptance` stopped before scene regeneration because its historical layout-9 terrain/water/route reference and sample count differ from the current world. `npm run visual:test` ran all four scenes and failed pixel comparisons against stored baselines. Actuals/diffs remain in `test-results/`; approved references were not overwritten. Human visual acceptance and the regenerated world-acceptance bundle remain open. At that checkpoint the Act 9 offshore-rod XP gate remained unchanged; the reported two-bream requirement is not reproduced by the current quest fan-out test.

### Clock-derived NPC stations — 2026-09-08

- **Implemented:** `npcs.ts` authors phase stops; `NpcPresentation.npcAnchorAt` is shared by nearby targeting, talking, turn-in validation, quest destinations and world station movement. Local beats are selected per phase and reset on relocation before distance culling. Elspeth retains the fresh daytime garden welcome. No saved state, migration, terrain change or new asset.
- **Automated pass:** direct `tsc --noEmit`; 160 tests across `npcSchedules`, `questDomain`, `questTracks`, `questExpansion`, `npcRecognition`, `npcStationBeat`, `questWayfinding`, `layoutEditorSourceRoundTrip`, `layoutEditorPatch`, `docContentCounts` and `docArchitectureClaims`. Targeted ESLint: no errors, two existing `no-explicit-any` warnings in unchanged patcher lines. `git diff --check` passed.
- **Editor:** scheduled stop addresses edit only their phase position; source tests preserve the harbor home anchor and other phases. Browser drag/drop was not exercised.
- **Browser observed:** a separate persistence-disabled `?debug&debugStart=farm` DEV session preserved the fresh Elspeth prompt. Advancing to dusk relocated the Neva cast into village stops; the tracker retargeted to Village Inn Porch and E opened Elspeth's dialogue and advanced the talk objective. At Sunreach Cove, Ines and Tomas became visible at their dusk stations and E opened Ines's dialogue. These used debug time/teleport, not a continuous walking journey or human visual acceptance.
- **World-life extension:** authored proximity barks now share recognition selection with dialogue; regional layer gains, adaptive themes, the existing contextual cues, window-material staggering and seasonal shader/fill tint are wired. Windows affect authored window surfaces, not windowless sheds or open halls. Icing audio follows derived protection, because there is no manual icing action.
- **Extension evidence:** requested npm typecheck passed. The full suite rerun recorded 1,820 passing tests and one startup terrain-parity timeout, plus a Vitest worker RPC timeout; it is not a green full-suite result. The isolated retry, with the live browser closed, passed both startup-parity tests (terrain parity completed in 43.5 seconds); the original broad run remains recorded as a timeout. Full lint exited with zero errors and 167 warnings. Focused audio/NPC tests passed before the extended run. Browser inspection observed an Elspeth bark, suppression during dialogue, active nighttime piano/region loops, full nighttime and zero daytime window emission, and autumn shader uniforms with no console errors. These are DEV/debug observations, not listening approval, performance certification or human visual acceptance. At that checkpoint the separate Act 9 offshore-rod XP issue remained unchanged.

### Guidance ownership and consistency refactor — 2026-09-06

- Refactored existing guidance in place: root routing and conflict resolution,
  one verification matrix, content/data ownership, current water/Art Yard/report
  semantics, audio implementation/design boundaries and player-choice/recovery
  acceptance. No new authority, gameplay implementation or gate waiver.
- `npm run content:validate` exits 0. Evidence:
  `output/docs-guidance-refactor-4nszlhi3/content-validation.log`. This validates
  the current registry; its totals remain in that output and `src/content/`.
- Structural checks pass: 36 newly referenced file paths, 33 distinct canonical
  section references and 21 npm aliases resolve. The migration ledger and the
  complete pre-reorganization evidence block match their retained originals
  exactly. Evidence: `output/docs-guidance-refactor-4nszlhi3/guidance-validation.json`.
  The scoped `git diff --check` passes; `refactor.diff` in that directory
  records this task's guidance changes against its starting snapshot.
- Save impact: **no**. Migration required: **no**. No gameplay suite, build,
  browser, listening, human visual or performance gate was run for this prose
  change. Concurrent runtime/art work keeps its own evidence and acceptance.

## Historical evidence

The block below preserves the previous checklist, including records added
while this refactor was underway. Its dates, counts, git snapshot, checkboxes,
“current” headings and green/red labels belong to those records. They are
retained evidence, not current routing, content totals or a new release status.
Use the owners and gate definitions above when deciding what to do next.

<details>
<summary>Retained implementation and verification records</summary>

<!-- RETAINED_EVIDENCE_START -->

# Neva Implementation Status Checklist

> Evidence snapshot: 2026-08-31 (Europe/Istanbul)
>
> This file is a status snapshot and working checklist, not a new design or technical authority. The authority order is owned by the repository-root `AGENTS.md`. Update this file in place when evidence changes; do not treat checked source presence as proof that a roadmap gate passed. Attach the narrowest current evidence and never carry a stale count, date, hash, or git state forward.

## 1. Audit basis

Canonical Markdown currently in `LLM/` (do not list deleted files):

- `AGENTS.md` (repository root — the routing authority)
- `LLM/AGENTS.md` (pointer to the root file; holds no independent rules)
- `LLM/01_GAME_FOUNDATIONS_ARCHITECTURE.md`
- `LLM/02_GAMEPLAY_SYSTEMS_IMPLEMENTATION.md`
- `LLM/03_PRODUCTION_ROADMAP_LLM_AGENT_PLAYBOOK.md`
- `LLM/04_ART_DIRECTION_BIBLE_PREMIUM_COZY_LOW_POLY.md`
- `LLM/06_AUDIO_AND_MUSIC_DESIGN_MASTER.md`
- `LLM/LLM_AGENT_ART_PIPELINE_INSTRUCTIONS.md`
- `LLM/ARCHEAGE_FARMING_SYSTEM.md`
- `LLM/BLENDER.md`
- `LLM/LAYOUT_EDITOR.md`
- `LLM/IMPLEMENTATION_STATUS_CHECKLIST.md`

There is no `05_` document; the numbering gap is historical and does not imply a
missing authority. `LLM/GAME_IMPROVEMENT_RECOMMENDATIONS.md` is **deleted** and is not an authority. `LLM/.DS_Store` is macOS metadata, not a project document.

Evidence labels used below:

- **Green** — the phase's current gate has direct passing evidence.
- **Partial** — meaningful implementation exists, but one or more required gate checks failed or were not run.
- **Red** — a required current gate is known to fail.
- **Not reached** — sequencing or prerequisite gates prevent a completion claim.
- **Recorded human evidence** — the repository records a human decision; this audit does not recreate or reinterpret it.

## Reference-led harbor rebuild — implementation evidence, review open

The harbor beach, shaded route, timber work shelters and rocky landing are implemented through the shared terrain owner, registered Blender coastal family and existing world placement path. Water uses the actual bed, depth absorption, shared surf/wetness and optional High-tier scene optics. The market approach reserves canopy clearance behind the normal camera boom, and the fixed boat berths retain catalog-hull clearance. The scoped visual overrides belong to `04` §8.1; the save-sensitive layout change and independent old-save fixture belong to `01` §6.1.

Evidence is collected under `output/coastal-rebuild-01a07252/`; `README.md`, `validation-summary.json` and `validation-provenance.json` identify the delivered captures, source inputs, measurements and limitations. `final-build.log` passes the normal package build, including asset synchronization and TypeScript, for the controlled source copy named by `final-source-input.json`. That copy preserves the last successfully built source plus the final coastal placements and published models. Later concurrent UI/audio/physics edits remain untouched in the working repository and are outside this build's runtime evidence; `verified-build.log` retains the failed attempt that caught an in-progress market JSX edit.

Focused checks pass: the five-file coastal/water group reports **26 tests** (`release-water-regressions.log`); the final collision and independent migration group reports **14 tests** (`final-coast-regressions.log`); the current HUD/admission files that failed earlier report **56 tests** (`release-prior-failure-recheck.log`). The five files containing the earlier migration/persistence timeouts pass **65 tests** in `final-focused-regressions.log`. These overlapping groups are not an aggregate suite count. The original full-suite run remains a failed historical run: **1,696 passes / 17 failures**, plus two worker RPC timeout errors (`final-tests.log`). Its observed assertion/timeout cases have passing focused follow-ups, but no clean full-suite rerun is claimed here. Whole-repository lint and the two production E2E assertions reproduced on the frozen baseline retain their separate limitations in the evidence report; focused coastal ESLint passes.

The selected catalog publication and clean regeneration determinism pass in `final-published-validation.log` and `final-coastal-determinism.log`; `selected-assets.json` identifies the selection and the generated report owns asset counts and budgets. `final-gameplay/measurements.json` records **12 reached route waypoints**, seven passing harbor interaction groups and no page/console/network errors. `final-farm/measurements.json` records UI planting and watering; `final-weather/measurements.json` records the actual dawn, midday, evening fog and rain conditions. Static comparisons use diagnostic relocation, while the continuous route uses real movement controls and the normal camera. Full Sunreach sailing, whole-app repeated teardown, human integrated visual approval and release certification remain separate, open gates.

## Current product and implementation update — 2026-08-30

- The human accepted the P12 product gate after manually playing the continuous authored quest chain through its end. This permits post-P12 progression/playability work. The automated continuous save/reload proof and Chromium/Firefox/WebKit release matrix remain open P15/P16 certification evidence.
- Current persistence versions and historical preservation boundaries belong to `01` §6/§6.1 and the live schema/layout owners. The earlier version numbers in this historical update are not a current-state declaration; do not duplicate the migration ledger here.
- Work is a hard, fully funded manual-production resource. Plant, water, harvest, fertilize, irrigate, processing start, basic cast, and sport hook spend their full discounted Work cost or make no gameplay mutation. Movement, boats, cargo, trade, quests, and dialogue are free.
- Harbor tackle progression is live: Willow → River → Heavy Sport → Offshore → Master. Purchases require the preceding rod plus the existing Fishing XP gate, auto-equip, persist, and are unavailable during active fishing; owned rods can be switched at the harbor.
- Harvest quality now scales awarded Farming XP at Common 1.0, Fine 1.1, Exceptional 1.25, and Prize 1.5. Produce-quality pricing remains deferred until inventory preserves quality lots.
- The live catalog/public manifest is synchronized; `generated/reports/asset_budget_report.json` owns its current counts and budget disposition. Catalog parity does not close the separate technical-art benchmark, strict-generation, determinism, or human gameplay-camera gates.

### Focused P13 quest and core-fun delta — 2026-08-31

- The authored story count is now **13**, owned by `src/content/quests.ts`: the accepted ten-quest P12 spine remains behaviorally intact and flows into a three-quest linear stewardship postscript for contract judgment, the field-pump capability, irrigation, fish-scrap fertilizer, and `knowledge.land_sea_cycle`.
- Quest costs and rewards are data-owned; rowboat commissioning no longer has a quest-ID branch. Successful contract, fertilizer, irrigation-installation, irrigation, rod-purchase, and boat-purchase events can advance only the matching active objective. Registry startup rejects duplicate quest/objective IDs, broken targets and locations, invalid costs, unsupported objective/location combinations, chain cycles, and unreachable entries.
- Older inactive epilogue saves that completed Quest 10 deterministically activate Quest 11 without replaying Quest 10 rewards. Save impact: **yes** because new stable IDs may enter existing quest/journal arrays. Schema migration: **no P13-specific migration**; v22 owns quality/knowledge backfill and the later v23 market rebase is independent. Focused reload/reconciliation tests cover the content-chain transition.
- Contract refill now preserves one attainable produce listing and, after rowboat access, one attainable fishing listing when eligible. The Expedition Board renders a pure steady/bold opportunity query with demand/value/deadline context and blockers for supplies, tackle, cargo, ice, and rough water. NPCs recognize selected milestones from existing completed-quest, feature, and knowledge state; no branch, schedule, transcript, or relationship state was added.
- Current focused evidence: the seven-file simulation/content gate (`questContentValidation`, `questExpansion`, `expeditionOpportunities`, `questDomain`, `gameLoopCadence`, `questPersistence`, and `p12VerticalSlice`) passes **7 files / 38 tests**. `tests/e2e/p13Stewardship.spec.ts` passes **1 Chrome test** through the real contract/dialogue/pump/processing/fertilizer/journal UI and verifies autosave plus reload; debug relocation keeps route traversal outside this narrow gate. First-time-player comprehension, continuous traversal, the browser matrix, balance, and release proof remain open.

### Focused P9 economy rework — 2026-09-01

- The market now rests at authored target supply, responds symmetrically to player gluts and shortages, and derives demand from pure world-seed/day/hour signals without consuming shared gameplay RNG. Single and bulk trades use exact marginal fills; quote totals match committed revenue and per-click Trading XP floors are removed.
- A 1.25 retail spread plus the best live wholesale floor closes immediate cross-market round trips. Contract rewards use gate-adjusted rest references, and the market modal renders simulation-owned buy/sell/fish/rod/contract DTOs rather than rebuilding formula or gate truth.
- Save impact: **yes**; schema v23 rebases authored commodity state without changing world, inventory, cargo, or unknown market content. Focused evidence passes **4 files / 57 tests**: `economyBalance`, `persistence`, `huntFixes2026`, and `expeditionOpportunities`. A narrow Chromium interaction at the village market quoted 10 Bait Worms at 50 G, labeled demand Steady, and committed the sale from 100 G to 150 G through the real UI event path. Continuous market-loop and human comprehension review remain open.

### Starter island mountains and finite spring — historical implementation evidence

- `NevaLandforms`, `NevaHeadwaters`, and the shared route registry now own the northern skyline, foothills, rocky spring, contour climb, and western overlook. `01` §6.1 owns the save/layout revision and preservation boundary. No quests, progression gates, asset publications, terrain-resolution increases, or Sunreach terrain changes are part of this pass.
- Focused evidence: `worldLayout` passes 35 tests; `nevaLandforms` passes 3; `starterIslandPreservation` passes its exact pre-change working-ground/anchor/route/lower-river/Sunreach comparison. `starterIslandTraversal` passes all four complete route directions with Rapier support, normal orbiting camera, and camera-ground clearance checked throughout.
- Final combined regression: 160/161 checks pass across 12 files. The existing first physics-setup case exceeds its 30-second timeout during that run, alongside a Vitest worker timeout; its isolated retry passes in 7.7 seconds without source changes. `output/starter-terrain-review/` retains both logs rather than treating the combined run as clean.
- Migration evidence: `terrainLayoutMigration` and `persistence` pass 57 tests, including historical validation, active fishing, mounts, valid/invalid boats, resource/RNG preservation, repeat loads, backup failure, and valid dry positions within the padded headwater rendering bounds. Neighboring mount, quest-persistence, and school-chum suites also pass. Water geometry, CPU gradients, profile/shader linkage, finite flow, dry clipping (including interpolated raised triangles outside the padded bounds), and patch ownership pass 16 focused tests.
- Generated art/UI check-only commands, source/tools TypeScript checking, and the frozen production build pass. The whole-repository typecheck run reports unrelated inspector/audit and character-animation test errors; its retained log owns the exact failures. Both island composition audits pass across seeds 0–63, and all protected layout preservation fields match.
- The isolated production-water probe compiles and renders both water shaders on hardware at low/medium/high without WebGL errors. Geometry counts show headwater refinement adds no draw calls; `output/starter-terrain-review/neva-water-probe.json` owns measured cost and timing. This is not a whole-game performance result.
- Frozen starter-terrain hardware acceptance is **blocked at startup**: the concurrent character catalog/model state raises `[HumanoidRig] char_player_a: missing root (Root)`. No gameplay scene or traversal capture completed. The frozen input manifest, failure report, startup log, and passing static reports are retained under `output/starter-terrain-review/`; the source stayed byte-stable during this attempt. Gameplay-camera inspection, whole-game quality-tier budgets, and final integrated appearance remain **Awaiting human game review**.

### Terrain-preservation audit repair

- `01` §10 owns the historical sampling and bounded harbor-correction contract. Original layout-10 samples/hashes remain intact; replaying the original Sunreach grid matches its original terrain/water/region hash and sample count. The report retains raw anchor/route inequality separately from the current contract instead of claiming the world is byte-identical to layout 10.
- `vitest run tests/unit/starterIslandPreservation.test.ts` passes **12 tests**, including rejection of additional route-point/width/endpoint and anchor-coordinate/height changes. `vite-node tools/world/run-neva-preservation-audit.ts` reports all current protected-field checks passing. This is a source audit, not the full world-acceptance gate.
- `vitest run tests/unit/physicsWorld.test.ts -t 'walks the village-harbor route to the market apron'` passes **one selected Rapier test**; the other cases were not run. No terrain, route, save schema, migration, or gameplay state changes belong to this audit repair. Concurrent world work and the remaining rendering/browser/release gates require their own evidence.
- `vitest run tests/simulation/mounts.test.ts` passes **11 tests** after replacing the blocked-dismount grid search with controlled lateral landing predicates. Failure preserves the full state and emits no disembark event; restoring the predicates allows exactly one successful disembark. Existing mounted/bridge traversal cases remain real Rapier tests. Initial attempts stopped during test collection while concurrently published crop models were absent; the passing run followed verification that all catalog model paths existed.

### Source-rig and ground-cover audit regressions

- The source-rig challenger, `characterEquipment`, and `humanoidRuntime` pass **42 tests** together. Published characters/tools replace the obsolete donor-rig fixture; actual source-mixer parity, moving-palm docking, existing missing-marker failures, and existing slope/contact cases pass. Evidence: `output/audit-repair/source-rig-regression.log`.
- `roadEnvironment` passes **5 tests**. Exact generation budgets remain checked separately from the final harbor-clearance projection. Grass must favor the causal cover field over three independently seeded uniform eligible-ground controls; flower proximity remains checked. The diagnostic in `output/audit-repair/grass-density-probe.jsonl` retains the observed density/proximity comparison rather than treating a lower fixed-radius neighbor fraction as proof of uniform scatter.
- `tsc --noEmit`, focused ESLint for both changed test files, and focused `git diff --check` pass. No runtime behavior, source assets, save schema, migration, or vegetation budget changed in this regression repair. The concurrent harbor implementation is not certified by these checks. Current production profiling, broader regression/build/browser evidence, authority reconciliation, and human visual acceptance remain open.

### Production GTAO depth-reuse audit repair

- `RendererPipeline` reuses the current scene depth for GTAO instead of redrawing geometry with an override-normal material. No assets, geometry, instance quotas, quality tuning, gameplay or saves changed in this repair. `04` §7 and Art Pipeline §5 own the visual and buffer-ownership contract.
- `gtaoSceneDepth` and `gpuFrameTimer` pass **7 tests** together; source TypeScript and focused ESLint pass. The new tests exercise real GTAO gathering/denoising, composer-buffer swaps, resize, shader-mode invalidation, explicit missing-depth failure and borrowed-depth disposal ownership. Evidence: `output/audit-repair/gtao-depth-regression.log` and `gtao-depth-types.log`.
- The fresh high-tier 1920×1080/DPR-1 production benchmark reports **290 draws / 871,104 triangles**, with no runtime/network errors. Its build passes, but its budget test still fails the preferred draw limit. `output/audit-repair/render-budget-depth-reuse.log` owns this narrow result; it is not cross-scene budget clearance.
- The previous frozen build contains concurrent water/harbor differences, so its visual comparison cannot isolate this repair. The controlled comparison instead serves the same candidate build and assets to both browser contexts, disabling only the depth-binding helper in the control's JavaScript response. No production test toggle or source mutation is used. `output/audit-repair/gtao-depth-controlled-review.json` and `gtao-depth-controlled-summary.json` record matching scene/camera identities, fixed time/weather, screenshots, per-frame draws/triangles and zero errors across both variants of all four gold views. Override-normal submissions are absent only in the depth-reuse variant. Pixel differences are diagnostic, not aesthetic approval; short GPU snapshots are not a sealed frame-time non-regression result.
- Wider gold views still exceed draw limits; harbor/coast also exceed the hard triangle ceiling. Keep the failing gate open and continue profiling remaining source-material, shadow and ground-cover submissions. These historical controlled captures also show the frozen benchmark player in a bind/T-pose in both variants; the separate zero-time repair below addresses that animator defect without changing these retained artifacts.
- At the time of these historical GTAO captures, retaining the composer in `no-post` conflicted with the then-documented bypass contract. Art Pipeline §13.3 now explicitly retains the High-tier scene-color/depth path for same-quality water optics. The capture-mode repair below verifies this current contract and fixes a separate stale-AO transition defect. Human gameplay-camera approval, broader regression/build evidence and release acceptance remain open.

### Zero-time humanoid pose repair

- `HumanoidAnimator` evaluates the first selected base and upper clips at full weight after construction or a full presentation reset, without advancing the clock. Subsequent transitions retain their existing blends. Art Pipeline §10 owns this presentation-only contract; gameplay, saves, migrations, source assets and benchmark cameras are unchanged.
- The new `humanoidInitialPose` regression reproduced **13 failures** before the fix. It now passes with `humanoidRuntime`, `animationController` and the source-rig challenger: **61 tests across four files**. Coverage includes all catalog characters, initial vehicle/carry/fishing layers, repeated zero-time updates, full resets and later contact blending. Evidence: `output/audit-repair/initial-pose-before.log` and `initial-pose-regression.log`.
- TypeScript, focused ESLint, the production build and focused diff checks pass. The frozen production browser check passes all four gold views at high quality: four samples per view retain time zero and match actual player arm transforms to an independently evaluated published idle clip, with no page, console or network errors. Evidence: `output/audit-repair/initial-pose-browser.json`, its script/log and companion screenshots. This proves the frozen initialization path, not complete gameplay animation, frame-time budgets, human visual approval or release acceptance.

### Capture-mode AO continuity repair

- `RendererPipeline` no longer advances AO refresh bookkeeping while GTAO is disabled, and invalidates its retained sample when capture mode changes. Starting in `no-post` or returning from it now gathers and denoises on the first `final` frame instead of compositing an uninitialized or stale target. Repeating the same mode retains normal reuse. Water targets, content, quality tuning, output conversion and all gameplay/save state are unchanged by this repair; Art Pipeline §13.3 owns the contract.
- `rendererCaptureMode` reproduces **four failures** before the fix. It passes with `coastalCapture`, `gtaoSceneDepth` and `gpuFrameTimer`: **15 tests across four files**. TypeScript, focused ESLint, focused diff checks and the isolated production build pass; the build retains its existing large-chunk warning. Evidence: `output/audit-repair/capture-mode-before.log`, `capture-mode-regression.log`, `capture-mode-types.log` and `capture-mode-build.log`.
- The frozen production Chrome check starts in `no-post` and switches twice to `final` in both bridge and harbor views at high quality, 1920×1080/DPR-1. All **64 recorded frames** preserve active independent water color/depth snapshots with exactly two texture copies per frame. Both returns in each view gather/denoise immediately; subsequent frames retain normal AO reuse, and `no-post` submits neither AO pass. Camera, seed, content counts, quality, time and weather match across modes; page/console/network errors are absent. Evidence: `output/audit-repair/capture-mode-browser.json`, its script/log and companion screenshots. This is narrow mode-transition/runtime evidence, not cross-scene performance clearance or human visual/release approval.

### Resumed regression and authority reconciliation

- `tsc --noEmit` passes after the HUD test updates; `output/audit-repair/hud-semantic-types.log` retains its output. No runtime TypeScript or visual design changed in this repair.
- The broad Vitest run in `output/audit-regression-TuXQIE/run.log` was stopped on the user's request and exited **143**. It has no complete suite result and must not be presented as a passing full run. After resumption, the five previously failing files reproduced **15 failures / 88 passes**; `output/audit-repair/resumed-failures.json` and its log own that baseline against the then-current worktree.
- The M2 HUD and M5 overlay failures were stale presentation assertions, not reasons to restore retired chrome. Calendar comparisons now tolerate React text separators while keeping the exact expected date/time; tool selection checks identify the selected button and four unselected peers; weather checks retain hazard identity/severity and priority. Vessel checks explicitly cover compact docked and expanded underway states instead of requiring sailing instruments on a docked boat. The two repaired files plus `hud_m1` pass **53 tests**. Evidence: `output/audit-repair/hud-semantic-regression.log`. Focused ESLint exits zero with four existing type warnings, and focused diff checks pass. Server-render tests are not browser layout or input proof.
- M1 assertions now follow the actual purse, compass rail, cargo badge and toolbar accessibility owners rather than retired classes or radar geometry. After this reconciliation, the remaining regression reproduced an `undefined Stance` accessible name. The toolbar now retains the Explorer label fallback without changing supplied slots, selection or simulation actions; Art Bible §17 owns this presentation contract. `adversarial_m1_hud`, `hud_m1` and `contextualHotbarActions` pass **63 tests across three files**. TypeScript passes; focused ESLint reports zero errors and five existing type warnings. Evidence: `output/audit-repair/hud-m1-semantic-before.log`, `hud-m1-semantic-regression.log`, `hud-m1-semantic-types.log` and `hud-m1-semantic-lint.log`. No save or migration changes are involved. Viewport/world-layout failures, complete regression evidence and cross-scene performance clearance remain open; this narrow check does not establish browser layout, real input, human visual approval or release readiness.
- A subsequent current-worktree recheck of `worldLayout` and `viewport_budget_m1_adversarial` reports **46 passes / two failures**, plus an unhandled Vitest worker `onTaskUpdate` timeout; the command exits one and is not clean validation. All 35 world-layout assertions report passing without further world changes in this pass. Both assertion failures are in viewport SSR checks against retired crest/currency ownership. The viewport suite also derives layout from copied legacy dimensions instead of measuring current DOM/CSS, so its passing geometry assertions do not establish responsive coverage. Evidence: `output/audit-repair/remaining-layout-regression.log`. Actual browser layout coverage and a clean runner result remain required.
- `02` now distinguishes the live mount gallop budget from deferred feeding/ownership features, removes obsolete main-only quest claims and points progression-table usage to its owning section. `03` follows Art Pipeline's current same-quality water-capture diagnostic contract and the content-owned story/track scope. Source evidence was generated through the existing content validator and `output/audit-repair/authority-snapshot.ts`, retained in `output/audit-regression-TuXQIE/`. This documents existing behavior without adding gameplay, changing saves, or certifying the complete authority set.

### Touch utility clearance and actual viewport measurements

- Browser hit-testing reproduced an on-foot landscape defect: at 844×390, the Interact/Use controls intercepted the satchel, journal and map button centers. The shared `mobile.css` reservation accounted for two action rows while `MobileControls` renders three. It now includes the third row and both gaps before lifting HUD controls. This is a presentation-only repair with no gameplay, save, migration or renderer-quality changes; Art Bible §17 owns the contract.
- `output/playwright/hud-viewport-audit.mjs` uses installed Chrome, current development DOM/CSS, automatic UI scaling, and isolated browser contexts. It measures visible HTML cluster bounds and descendant overflow, excluding clipped SVG atlas resources, and records actual center-point hit targets. The retained before/after JSON and logs are `hud-viewport-before.*` and `hud-viewport-after.*` in that directory. Utility-to-action clearance changes from **−28.8125 px to 10 px**. All three utility dialogs open through actual touch taps after the repair; Escape dismisses each. The portrait orientation gate remains present. Seven targeted checks pass, along with script syntax and focused diff checks.
- Five normal desktop viewports (1920×1080, 1366×768, 1280×720, 820×600 and 1280×620) have no measured cluster overlap, off-screen bounds or intercepted enabled HUD button centers. Conservative cluster-box coverage is **12.4–18.9%**. This is an initial-farm desktop result, not high-load, placement, vessel, every UI scale, real-device or production performance clearance.
- The complete responsive gate remains open: short-landscape cluster envelopes still intersect. These historical captures also reproduced portrait menu interception and exposed obsolete viewport unit assertions; the following repair supersedes those two findings. The runs record aborted asset requests (no page, console or HTTP errors in the after run); do not present them as clean network validation. No full suite, production build or human visual/release approval is implied.

### Portrait access and durable viewport regressions

- The existing portrait menu button now moves above the orientation gate alongside the clock; covered normal-HUD controls are removed from display and keyboard focus. Rotating back restores their ordinary positions and actions. `coastal.css` owns the scoped rule and `MicroMenuPurseBar` supplies its semantic menu class; no duplicate menu, gameplay action, save or migration is introduced. Art Bible §17 owns this presentation contract.
- The new `touch HUD keeps utility actions reachable through landscape and portrait rotation` E2E first fails on actual portrait menu hit-testing. After the repair it passes together with the updated `field-journal HUD stays compact and pause exposes contextual records`: **two Chrome E2Es**. The desktop flow measures seven real viewports, including both short/narrow breakpoints, below the 25% conservative cluster-box ceiling with no overlap, off-screen bounds or intercepted enabled HUD buttons. It retains actual pause-to-journal/map/ledger/satchel navigation and focus restoration. The touch flow opens all three utilities, rotates to portrait, opens pause and forecast, dismisses them and restores landscape controls. Evidence: `output/playwright/portrait-before.log`, `portrait-after.log`, `hud-responsive-regression.log` and the final portrait screenshot under that run's artifact directory.
- `viewport_budget_m1_adversarial` now retains its two meaningful large-content semantic cases rather than eleven copied-geometry/CSS-string assertions. Those cases pass after React text-separator normalization; `hud_m1` separately passes its 26 neighboring cases. Evidence: `output/audit-repair/viewport-semantic-regression.log` and `viewport-semantic-final.log`. TypeScript, focused ESLint and focused diff checks pass. Roadmap §28 assigns current browser coverage to `control-foundation.spec.ts`; high-load, vessel, placement, every UI-scale preference, other gameplay modes, real-device and cross-scene performance gates remain separate and open. The prior aborted-request finding is not cleared by these interaction checks.

### Full-regression baseline and focused follow-up

- The direct baseline finished with **173 passing and 11 failing files; 1,682 passing and 20 failing tests; five worker `onTaskUpdate` RPC timeout errors**. Evidence: `output/audit-full-regression-NS4UdT/run.log` and `results.json`. The failures split into six stale HUD assertions, nine migration test timeouts, one simulation-construction hook timeout and four art-admission failures. This contended shared-worktree run is not a passing full-suite or clean-source certificate. Runtime edits by this audit were withheld until it finished; the already-completed HUD test files were reconciled separately during the run.
- `empirical_m2_hud` and `hudNotifications` now exercise the current almanac, storm identity/severity, menu shortcut, Work meter values, exact selected tool and readout. Clock cases render the live DTO-to-HUD path with independent boundary expectations instead of a retired decoration and copied formula. The unavailable-seed assertion targets slot 2 specifically, so an unrelated empty fertilizer slot cannot satisfy it. A separate direct run passes **41 tests in two files**; TypeScript and focused diff checks pass, and ESLint reports zero errors with one pre-existing `any` fixture warning. Evidence: `output/audit-repair/hud-empirical-final.log`, `hud-empirical-types.log` and `hud-empirical-lint.log`. This is semantic regression coverage, not fresh browser interaction or visual proof.
- The initial cold Node inspector profile confirmed that collision recovery eagerly generated presentation cover: 34.73 seconds in migration, with 25.17 seconds of inclusive sampled time in the two cover generators. A concurrent implementation then supplied `createWorldStaticPlacements` and deferred the renderer's cover getter; this audit preserved that edit and added `terrainMigrationComposition` coverage for the real seeded static path, shared cache, input immutability, valid output and idempotence. Architecture §7 owns the contract; no schema/layout bump or timeout increase is needed.
- Concurrent coral placement changes altered the initial full-static hash, so a second controlled comparison loads the pre-optimization composition and migration modules from a retained Git reference through the output-only `migration-control.vite.config.ts`, without changing the checkout. The matched-content pair has identical fixture, migrated-envelope, complete static-placement, ground-cover and collision hashes. Cold migration elapsed time is **19.40 → 5.97 seconds**, with process CPU **18.88 → 4.73 seconds**; no cover-generator samples remain inside migration. Cover still costs 33.46 seconds on its later first access in the after sample. These single contended Node samples show removed migration work, not a browser FPS or end-to-end startup improvement. Evidence and exact reference: `output/audit-repair/profile-cold-migration.ts`, `migration-control.vite.config.ts`, `cold-migration-control-before.json`, `cold-migration-control-after.json`, their CPU profiles and `cold-migration-control-comparison.json`.
- The targeted follow-up passes **168 tests in nine files**, including every baseline migration/hook-timeout file and the new composition boundary tests, with the existing limits unchanged. Evidence: `output/audit-repair/migration-static-regression.log`. Direct TypeScript, focused ESLint and diff checks pass (`migration-static-types.log`, `migration-static-lint.log`). The baseline RPC errors are not cleared by this focused run, and no new full-suite pass, browser/visual acceptance or release certification is claimed.
- The four `artAdmission` failures reproduce independently in `output/audit-repair/art-admission-before.log`: the positive fixture declares an idle loop but ends 0.10 m from its initial pose. Its corrected three-key motion returns to the initial pose without removing animation; added raw and Meshopt negative cases retain the broken loop and assert rejection with source bytes, both GLB destinations and both manifests unchanged. The existing production validator is unchanged. **13 admission tests pass**, and **12 surface-contract tests pass**; focused ESLint and diff checks pass. The direct TypeScript run exits 2 because a concurrently created `tests/unit/__probe_heightfield.test.ts` has unused `ix` and `iz` variables; this audit leaves that probe untouched and does not claim a passing current typecheck. Evidence: `art-admission-after.log`, `art-surface-regression.log`, `art-admission-types.log` and `art-admission-lint.log` under `output/audit-repair/`. These tests use disposable fixture roots only: no production asset generation, publication or human visual approval is implied.
- Architecture §2.1 is reconciled against the existing `QuestState`, `questTracks.ts`, v29 migration and `02` narrative/persistence contract: per-track cursors and focused-track ownership replace the obsolete single-chain/reserved-dialogue-field prose. Exact story counts remain with content owners, and the ledger pointer now names §6.1. The copied published-asset count in §16 is replaced with the manifest/catalog ownership reference. This is documentation-only reconciliation, not new content, a save change or a new migration. Source inspection and focused diff checks verify the edits; the preceding quest-persistence regression evidence is not a new browser or narrative acceptance run.
- The direct full-suite follow-up finishes with exit 1: **183 passing / four failing files, 1,705 passing / four failing tests, and two worker `onTaskUpdate` RPC errors**. Evidence: `output/audit-full-followup-nFqGQY/run.log` and `results.json`. All original baseline failure groups pass in this run. The remaining failures are one temporary probe removed after discovery, an undefined stair helper observed by another temporary probe and two dock-traversal cases, and a 30-second road-cover generation timeout. The stair helper is present in the later source; the two temporary probes are no longer present. These observations require focused current-source rechecks, not edits to restore removed probes or relaxed test limits.
- The same follow-up's `inputs-before.json`, `inputs-after.json` and `input-comparison.json` identify **18 changed inputs**, including world/physics sources, temporary probes and this audit's audio repair. The newly added audio lifecycle suite was not discovered by that run and is verified separately below. `output/audit-repair/snapshot-regression-inputs.mjs` owns input capture; endpoint hashes cannot prove the absence of transient intermediate edits. This is not a clean-source or passing full-suite certificate. No timeout, worker-RPC setting or test exclusion is relaxed.
- The focused current-source recheck of `physicsWorld` and `roadEnvironment` exits 1 with **33 passes / two timeouts and one worker RPC error**. All **30 physics tests**, including both dock-stair directions, pass without further physics edits by this audit. Road-cover determinism and habitat/clustering exceed the unchanged 30-second limit (observed durations 66.35 and 48.24 seconds). CPU contention from simultaneous Blender, browser and test work is observed, but does not establish the sole cause; ground-cover cost and clean runner evidence remain open. Evidence: `output/audit-repair/followup-failures.log`, `followup-failures.json` and `followup-failures-inputs-before.json` / `followup-failures-inputs-after.json`. World/physics/test endpoint hashes match in this focused interval; separate UI and checklist edits occurred, and transient intermediate changes are still not ruled out. No restored diagnostic probes, timeout increases, test exclusions or production performance clearance are implied.

### Audio unlock and visibility recovery

- A controlled reproduction using the production `AudioManager` first unlocks, suspends the context, then unlocks again. Before the fix, the cached successful promise leaves the context suspended with only one resume call; afterward the same reproduction reaches running with two calls. Evidence: `output/audit-repair/audio-unlock-before.log` (exit 1) and `audio-unlock-after.log` (exit 0). The fixture controls context state; it is not browser playback proof.
- `AudioManager.unlock()` now caches only pending work, deduplicates concurrent resume attempts and permits retry after suspension or rejection. Visibility failures are handled, and completion guards prevent disposed managers from restarting music, beds or requested action loops. `06` §2.3 owns this runtime lifecycle boundary. Save impact: **no**; migration required: **no**. No cue, bus, manifest, audio asset or gameplay contract changes.
- The lifecycle suite and neighboring manager/parameter suites pass **11 tests across three files**, including positioned action-loop recovery, rejected suspend/resume and disposal during a pending resume. Evidence: `output/audit-repair/audio-lifecycle-final.log`. Direct TypeScript and focused ESLint previously completed with exit 0 in `audio-lifecycle-types.log` and `audio-lifecycle-lint.log`; these precede the final additional action-loop test, whose focused ESLint also exits 0 (`audio-lifecycle-final-lint.log`). Focused diff checks pass. The later TypeScript result clears the earlier temporary heightfield-probe error without changing or relabeling its failed historical log.
- A real WebAudio check in the Codex in-app browser exercises the production manager through an actual button click, suspend, second unlock and concurrent unlocks: every expected resumed state is running, with no observed console warnings/errors. Evidence: `output/playwright/audio-lifecycle/probe.html` and `browser-result.json`. Theme and ambience playback are suppressed in this isolated probe. This is not full-game listening, mix approval, real browser visibility-policy validation, mobile/cross-browser acceptance or release certification.

### Cold layout decoupling and physics KCC/mount feel repair — 2026-09-05

- `WorldEnvironmentLayout` now exports `createWorldStaticPlacements(worldSeed)` with a dedicated static placements cache, and evaluates `groundCoverPlacements` lazily on `WorldEnvironmentLayout`.
- `migrateTerrainLayout13` consumes `createWorldStaticPlacements` directly, removing the expensive ground-cover generation during terrain migrations without altering collision proxy truth or hashes.
- `PhysicsWorld` eliminates the redundant pre-KCC `resolveWalkableSlide` pass so Rapier calculates physical collision slide first, followed by a single walkability projection pass on the computed movement. This eliminates shoreline sticking and wall-stop jitter at waterlines and cliff edges.
- `PhysicsWorld` enables mount ground snapping at `MOUNT_GROUND_SNAP_METERS = 0.26` (previously disabled), eliminating airborne float and stair-riser catches during 7.2 m/s gallop.
- `PhysicsWorld` updates scene queries once before the boat loop in `step` rather than per boat on every tick in `resolveBoat`, eliminating harbor frame hitches.
- Evidence: `tests/simulation/persistence.test.ts` passes **38/38 tests** cleanly (v24 calendar fixture test timeout reduced from >30s failure to 6.2s pass); `tests/unit/physicsWorld.test.ts` passes **30/30 tests**; `tests/simulation/mounts.test.ts` passes **11/11 tests**; `tests/unit/assetPlacementAudit.test.ts` passes **6/6 tests**; `npm run typecheck` passes with zero errors.

### P12 route harness repair — full record, 2026-09-06

Running the P12 continuous browser route to find out why it was blocked produced **nineteen defects, all in the test artifact and none in gameplay code**. They fall into four classes.

**Stale against the P14 interface overhaul (9).** Other specs were realigned to the renamed copy; this one was missed. `Guild Satchel` → `Satchel`; `complementary "Active Quest Objective"` → `region "Active objective"`; market ledger `tab "Sell"`/`tab "Docked Fish"` → `button "Sell"`/`button "Fish hold"`; `/Sell All Fish/` → the rendered `Sell all fish`; journal `dialog /Cove Chronicle/` → `Field Journal`; `Completed Chronicles` → `Completed stories`; and the two market prompts `Browse the produce stall` and `Trade with Maeve`, neither of which exists in `src` — `GameApp` emits `[E] Trade at ${marketName}`.

**Wall-clock pacing (5 loops).** `walkTo`, `boatTo`, `holdBridgeAxis`, `castAndResolveBasicFishing` and `landSportFishWithKeyboard` measured progress and budgets in real time. The application caps its physics accumulator, so a starved browser drops simulation time: the actor genuinely moves and drains less per real second and the assertions fail with nothing wrong in the runtime. All five now pace on delivered fixed steps. Architecture §11 owns the contract and `03` §28 carries the boundary row.

**Miscalibration (5).** A 2 cm and a 12 cm bridge-alignment tolerance, both unreachable by digital tap input and both replaced with 0.3 m derived from the deck's 1.9 m collision half-extent; an 8 s bridge budget against an 18.4 m crossing, now derived from distance and the canonical walk speed; a 120 s sport-fight window that expired with the trout at 18% energy and 0.6 m, reported "Within reach"; and a per-sample progress comparison that read a slow, correct approach as no progress at all, now measured against closest approach.

**Stale world and interaction assumptions (5).** `Digit5` was pressed to arm the rod, but P14 made the hotbar stance-based: at a fishing access the stance is `angling` and the rod is slot 1, while in `explorer` slot 5 opens the Field Journal — which froze movement and hid the very prompt the step then waited for. The cast itself was attempted at the bridge exit, east of where the river corridor's authored access ends. A held Space carried across the minigame's phase change, so the landing card's Collect arrived as an auto-repeat and never fired. And two route legs ran through authored collision: the harbor approach and return crossed `building_coastal_store_a` (80.3, 49.3), with the waypoint (82, 50) inside its footprint, and the return sail cut through the pier keep-out spanning x = 74..78 below z = 79.

**Method note, and a correction to it.** A first audit pass validated all 40 waypoints with `WorldLayout.isWalkable` and reported zero problems; the next run then failed with `blocked=true` at the harbor. `isWalkable` describes terrain and water and knows nothing about authored props, so a waypoint can sit inside a building and still pass. The corrected audit samples every 0.25 m along each **segment** and tests it with `staticPoseIsClear` against proxies projected through `projectAssetCollision`, which is the same path the physics world uses. That version found both obstructed leg groups. Its limitation: proxies come from `createWorldStaticPlacements`, so farm fences and landmark prefabs assembled from the render scene graph are still not covered.

**What the game did, unaided.** Fresh save through planting, watering and harvesting three wheat, composting, two mill runs, chum, three bridge crossings, five NPC dialogue handoffs, quest progression into Act 5, arming a rod through the contextual hotbar, a full basic-fishing minigame landing a River Perch, sailing to the lake, chumming a school and fighting a Rainbow Trout to landing.

**Open and explicitly unresolved.** Two stalls resist static explanation and were not guessed at: a player position reported at (-73.11, -58.81) while the walk targeted a route point 40 m away, and a boat halted at (23.21, 100.31) on a heading whose whole path tests sailable with `blocked=false`. Both traversal loops now trace position, speed, heading, keys and blocked state once a stall passes half its threshold, so the next occurrence carries a diagnosis. **No completed end-to-end run exists; the closing legs past the sport catch are unproven, and nothing here is a passing P12 automated gate.**

### Browser traversal harness paced on simulation steps — 2026-09-06

The P12 continuous browser route is the gate behind most of section 4's open boxes. Running it exposed two harness faults and no gameplay fault.

- **A stale copy assertion.** `p12VerticalSlice.spec.ts` expected `Guild Satchel`; the P14 overhaul renamed it to `Satchel`, and `Guild` no longer appears anywhere in `src/ui`. The satchel opened correctly with its wheat seeds and a working Plant action, so a single renamed word was failing the whole gate. The other specs had been realigned to the P14 copy; this one was missed.
- **Progress was measured in wall-clock time.** `GameApp` caps its physics accumulator, so a browser starved of CPU drops simulation time rather than catching it up: the player really does move less per real second. `walkTo` declared a stall after 18 samples of a 130 ms hold, which a loaded machine trips while the game is behaving correctly. **Observed directly:** the same tree stalled at `(-65.00, -60.48)` after about 60 seconds while a full Vitest suite competed for CPU, and on a quiet machine walked through that point and went on to water and harvest all three crops. Supporting evidence that the game was never at fault: the identical walk runs clean in Node at a constant 1.6 m/s, the nearest static placement is a rabbit 3.86 m away, the authored fence gap is 0.7 m behind the reported stall, and the harness camera basis matches `cameraRelativeMovement` exactly.

`GameApp` now publishes a monotonic count of delivered fixed steps as `data-physics-steps` and `NevaDebugSnapshot.physicsStepCount` (DEV evidence, never serialized). Every load-sensitive loop is paced on it: `walkTo`, `boatTo`, `holdBridgeAxis`, `castAndResolveBasicFishing` and `landSportFishWithKeyboard` in the P12 spec, and `hold`/`waitForSteps` in `control-foundation.spec.ts`. Each threshold is the wall-clock value it replaces converted at the fixed rate, so a genuine stall still fails as quickly as before. Wall clock now survives in exactly one place per spec, a watchdog that fails a page delivering **no** step for 60 seconds — one step per second passes, zero does not. The route budget moved from 900 s to 3,600 s because both this tree and a clean `HEAD` baseline expired at the Act 2 compost step, failing on the clock rather than on the game; overshooting is cheap now that hangs are caught by the watchdog instead of the ceiling.

The same fault explains the four historical `control-foundation` movement/gait/placement blockers recorded above, which have been carried as suspected physics defects: the sprint case holds Shift+W for 1,100 ms of real time and then asserts stamina drained by more than 6, but stamina is spent per step. That attribution is a **reasoned inference from the shared mechanism, not yet a rerun result**.

Evidence and its limits. The reworked route reaches **past** the point where both 900 s runs expired: it plants three wheat, waters and harvests all three, and completes `step.act2_compost_worms` with `physicsStepCount` visible at 4,351 in the live snapshot. That proves the pacing change is wired correctly and that the previous failures were budget and load artifacts. It does **not** prove the full P12 route passes — the run was still in progress when this was recorded, the later river/harbor/sail/sport legs are unproven, and any defect they surface is the gate finally reporting rather than a regression. One consequence to expect: because slow machines are now given the simulated time they are owed, a contended run takes proportionally longer in wall-clock and may need more than the new budget. Typecheck and ESLint pass on every changed file.

### Physics query cost — profiled and reduced, 2026-09-06

A profile of the physics layer, rather than a reading of it, found the cost was almost entirely in redundant queries.

- **`hasLineOfSight` cost 6.5 ms per call — roughly 40% of a 60 Hz frame.** `world.projectPoint` was 99.5% of it, and isolating the cause showed the two terrain heightfield colliders were the whole of that: excluding them took the query from 6.50 ms to 0.0097 ms. `colliderBelongsToEndpoint` already rejects terrain, so the projection was computed and discarded every time. The query now filters terrain by collider handle, which cannot change the result. `hasLineOfSight` measures 0.035 ms. `InteractionTargetResolver` calls this once per line-of-sight candidate and `evaluateInteractionTarget` runs at up to 15 Hz, so this was several milliseconds of avoidable work per second whenever an NPC was a candidate.
- **The physics step was about 94% layout sampling.** Instrumented call counting showed 6.1 `traversalSurfaceSample` calls per on-foot step and zero height-only calls, at roughly 0.154 ms each. Five of the six discarded the normal that `traversalSurfaceSample` builds from four extra neighbour samples. `traversalSurfaceHeight` now takes the raw sample directly instead of going through the full one — the sampled height is identical by construction — and the step's height-only queries use it. The resolved point, the one place a normal is genuinely needed, is sampled once and shared between the ground height and the contact evidence. Per-step layout work: **6.1 full samples → 4 height-only plus 1 full**, about 0.79 ms to 0.32 ms.
- **`terrainSurface` derived a normal the caller already held.** `terrainSurfaceSample` has always accepted `sampledNormalY`; `terrainSurface` did not forward one, so every contact-surface classification paid four extra terrain height samples. It now accepts and forwards it, and the physics step passes the normal from the sample it just took. Cost falls from 0.400 ms to 0.046 ms. Over 11,643 sampled terrain and road points the two normals pick a different material at **2 points (0.017%)**, both sitting exactly on the grass/cliff slope threshold; the traversal normal describes the surface actually under the capsule, so it is the better evidence at those points.
- A 64-entry direct-mapped memo for the raw traversal sample was implemented, measured and **removed**: consecutive queries are never at identical coordinates, so it added hashing and store cost without hits.

Evidence and its limits. Ratios were measured inside a single process so both variants share machine conditions: height-only query 3.9x, `terrainSurface` with a known normal 4.9x, `projectPoint` 310x under load, and 4.2x / 8.6x / 670x in a quieter run. Absolute milliseconds moved by a factor of four to six between runs because another session was working in this tree throughout, so **no wall-clock or end-to-end claim is made** — the reported figures are single-process A/B ratios and noise-free call counts, not browser frame times. `tests/simulation/physicsTraversal.test.ts` did drop from 11.1 s to 4.5 s on the same tests, which corroborates the step reduction without proving a frame-rate figure. Correctness: **82 tests in 10 files** pass (physics, traversal, mounts, edge cases, collision debug, terrain snapping, harbor coast, terrain and road surface material), and **72 tests in 4 files** across persistence and all three terrain migrations. ESLint reports zero problems and no changed file produces a TypeScript error.

### Physics traversal, harbor stair authority and commit transaction — 2026-09-06

Two premises of the movement-feel plan were measured before being acted on, and both failed:

- **The terrain heightfield triangulation was already aligned.** A Rapier heightfield probe with a saddle cell reproduced `sampleTraversalBasePlane`'s `u + v <= 1` split exactly at six interior points (0.5000/0.7000/0.7500 against the alternative diagonal's 0.0000/0.5000/0.5500), and confirmed the first sample index maps to world X. No change was made; the agreement is now locked by a regression test rather than left to inspection.
- **No collision proxy the terrain migrations see carries pitch or roll.** All 54 proxies from `harborCoastCollisionProxies(createWorldStaticPlacements(seed))` are pure yaw, for which the existing closed-form footprint in `staticPoseIsClear` is exact. A full 3D OBB rewrite would have changed no migration outcome, and rewriting the flat-capped-cylinder actor model as a true capsule would have made low steps block during save recovery. Instead the pure-yaw assumption is now detected and a tilted box falls back to its world-axis-aligned bounds, which can only over-block.

The defects that measurement did confirm are fixed:

- **Dock stair support diverged from the authored boxes by up to 0.158 m.** `WorldLayout` sampled a linear ramp from the stair base to the deck while the catalog `dock_straight_a` primitives present five discrete treads at asset Y 2.61/2.43/2.25/2.07/1.89. The ramp floated up to 0.158 m above a tread mid-run and dipped 0.065 m below one at each riser, and its approach blend sat up to 0.078 m below the beach shelf it was blending into. `rawTraversalSurfaceSample` now mirrors the tread profile and takes the higher of tread and terrain; the approach reports `terrain`. Measured divergence against a downward ray into the live physics world across the whole stair run and approach: **0.158 m → 0.0000 m**. Pier stairs were also removed from the finite-difference normal case, since a 0.18 m riser over a 0.45 m sample read as a 22-degree slope on a flat tread.
- **A rejected `physics.commit` left Rapier permanently ahead of `GameState`.** `step` recorded its candidate pose as the synchronisation baseline unconditionally, so a rejected frame guaranteed a mismatch and a velocity-clearing resynchronisation on the next step. `PhysicsAdapter.onCommitResult(success)` now closes the transaction, `PhysicsWorld` rewinds body translation, velocities, rotation, grounded state and baseline on rejection, and `GameApp.updateMovement` reports the outcome. The teleport commit sites open no transaction and are unchanged.
- **`world.step()` ran every frame over a world with no dynamic bodies.** Only fixed and kinematic-position-based bodies exist; the character controller runs its own queries and no contact events are consumed. The step is now gated on a recomputed dynamic-body count, so a genuine dynamic body re-enables it without anyone remembering the gate. `dispose()` additionally clears the debug collider map, static prop list and staged transaction, and `setSlideEnabled(true)` is stated rather than left to the Rapier default.

**`computedGrounded()` was deliberately not made the sole grounded authority.** Instrumented over 200 frames per case, Rapier reports grounded on 99.5% of on-foot frames on open farm ground but only **71%** on a dock stair climb, with the layout's support fallback covering 32 of the 58 remaining frames — the controller loses contact across each riser. Removing the fallback would report airborne for roughly a third of every staircase. Mounts are pinned to the traversal surface rather than integrated under gravity and have no way to recover from a spurious airborne frame, so the mounted override stays. The measurement and the reasoning are recorded at the call site and in Architecture §11. Collision-group bitmasks were also left alone: no filtering requirement exists, and introducing groups would change player-sweep, boat-cast and camera-sweep query semantics with no test able to catch a mistake.

Evidence. New `tests/simulation/physicsTraversal.test.ts` passes **7 tests**, covering heightfield-versus-ray agreement within 0.02 m over 200+ off-grid interior samples, dock tread agreement plus a staircase-shape assertion, a foot traversal up and back down the stairs, a 7.2 m/s gallop into a nine-section fence wall checked for tunnelling every frame, a full-throttle hull held outside the pier pilings, and both the rollback and a control proving the resynchronisation hitch is real when the outcome is not reported. The affected suites pass together: `physicsTraversal`, `physicsWorld`, `physicsEdgeCases`, `mounts`, `harborCoast`, `harborCoastMigration`, `collisionDebug` and `terrainSnapping` at **78 tests in 8 files**; `persistence`, `terrainLayoutMigration`, `terrainLayout12Migration` and `terrainMigrationComposition` at **72 tests in 4 files**. ESLint reports zero problems on every changed file, and none of the changed files produce a TypeScript error. This is Node-level physics and layout evidence; no browser frame-time, visual or full-suite claim is made, and the reported harbor query cost was not measured end to end. An earlier single `roadEnvironment` timeout reproduced only under concurrent load and passes at 21 s in isolation both before and after these changes.

### Causal world remediation — 2026-09-01

- `WorldLayout` revision 9 owns a longitudinal asymmetric river profile, moving outside-bend thalweg, independent bank/floodplain sides, bend erosion/deposition, estuary continuity, district fields, and reserved same-side fishing approaches. Terrain, surface weights, wetness, placement validity, navigation, region mapping, and fishing access consume those shared causes.
- `WorldCompositionField` replaces habitat-cell coverage and inherited category placement with district/habitat/route/opening fields, independent category streams, stable priority-prefix IDs, and explicit core/edge/isolate/landmark/riparian/route-frame tags. Seeded placement overrides are empty; authored landmarks and working props remain.
- Save impact: **yes**. Migration required: **yes**. Schema v24 migrates layout 8 → 9, preserves valid X/Z and unrelated progression/economy/cargo/crop/boat/RNG truth, and relocates only land poses invalidated by the new water footprint. The remainder of the migration chain is recorded in `01` §6.1. `tests/fixtures/save_v23_layout8.json` is the retained fixture.
- The additive `world:acceptance` path hashes inputs, runs check-only adapter gates, builds and serves one static production bundle, records deterministic and hardware lanes, exposes true same-content no-post and causal-field overlays, collects non-blocking GPU timer queries and render-target inventory, and writes only below `output/world-alignment/<digest>/`. It cannot update approved screenshots or substitute for human gameplay-camera approval.
- Current narrow evidence: schema/layout migration coverage passes in `tests/simulation/persistence.test.ts` (**37 tests**); GPU timer failure/success states pass in `tests/unit/gpuFrameTimer.test.ts` (**4 tests**); direct `tsc --noEmit` and acceptance-script syntax pass. The complete seed `0–63` source audit passes: seed 42 repeats the same placement hash, the strongest 22 m ratio is `1.073`, the strongest 5.55 m ratio is `0.118`, the weakest district CV is `0.585`, and every seed passes district ordering, opening, role, route, fishing-component, and access-clearance gates. The complete frozen build/software/hardware capture remains a separate acceptance result, not implied by these source checks.

### Character source restoration and interaction repair

- The catalog character family uses immutable licensed source anatomy, complete source joints/weights/UVs/material regions/split normals, uniform coordinate conversion and source-timestamped peaceful performances. Catalog `humanoidAuthoring` and `humanoidRig` own offline preparation and runtime binding. Obsolete callable chibi humanoid generators are retired; the registered imported-source path, strict validation, lossless optimization and atomic publication remain authoritative.
- Final selected generation, semantic determinism and atomic local publication pass. `output/character-restoration/publication-parity.json` owns current cache/generated/public/manifest/source parity; independent source comparisons and `output/character-restoration/exported-characters.json` pass **50 actual-export tests**. The generated budget report owns dispositions.
- The complete generated acceptance record at `output/character-restoration/acceptance.json` covers every catalog action on every character and links timing, source/authored origin, contacts, equipment, transitions, both-LOD mechanics, raw/runtime review frames and current hashes. Added role garments pass **36 action/LOD cases across 1,900 evaluated poses** with zero intersections; tied-hair geometry and identity evidence remain separately hash-pinned. Integrated appearance and motion remain **Awaiting human game review**.
- Shared source finger grasps, independently parented feet, bounded rotation-only limb constraints, one animation clock, explicit transitions, signed acceleration, transient NPC movement, correct equipment lifetimes and simulation-owned commit/cancellation rules are implemented. Runtime checks pass **50 tests**; current final-public equipment/support checks pass **9 tests** with **184** measured contacts. Real-input dialogue, both mount sides, skiff transfer, collision, crop cancellation/commit, reduced motion, riding and reload evidence is linked from the acceptance record. Rapier surface checks cover bridge approaches/crossings, farm gateway, dock clearance and grounded jump; these focused checks do not certify every world route.
- Conservative character-local culling envelopes pass **2,682 exported poses**, **14 post-pose tool/boat/mount cases**, close inspection and off-screen rejection. The correction reduces measured actor submissions from **162–182** mean draw calls to **76–114** across farm/harbor quality tiers. The broader scenes still exceed the existing all-pass draw/triangle ceilings even with character geometry hidden; `output/character-restoration/performance/report.json` and `report-before-bounds.json` retain the current hardware evidence without treating this as character or release certification.
- Save impact: **no**. Migration required: **no**. The final production build and TypeScript check pass; the existing main/Rapier chunk-size warning remains. Human integrated-game review is the final appearance and motion gate.

### Tools upgrade evidence — 2026-08-30

- The unified `npm run tools --` entrypoint routes catalog art, the DEV layout editor, UI atlas publication, audio preparation, tests, CI, and allowlisted generated-output cleanup.
- The F2 editor now uses BVH-backed terrain snapping with slope rejection and a 100-item transactional undo/redo history. Drag/rotate is coalesced at drop; paste/delete retain stable IDs and reversible source/live-scene operations. Save impact: **no**. Migration required: **no**.
- The UI atlas publishes lossless PNG/WebP pages plus generated frame metadata; `AtlasImage` consumes those frames while preserving an ordinary-image fallback.
- The bundled runtime audio has been prepared through the canonical seven semantic roles. Spatial sources are mono, other sources are stereo, cue ranges are preserved, and manifest hash/duration/channel parity is checked mechanically. Human in-game listening/mix review remains open.
- The deterministic Playwright harness compares bridge, starter-farm, harbor, and coast gameplay-camera captures after a render-ready handshake. Passing pixels are regression evidence only; they do not supersede the recorded human gold decision or approve later visual changes.
- Current full-suite evidence: asset/UI codegen is synchronized against the catalog/public manifest; typecheck passes; lint has 0 errors and 45 warnings; 100 Vitest files / 969 tests pass; the production bundle builds; all 64 runtime audio sources pass hash/duration/channel parity; and all four deterministic Chromium visual scenes match their current baselines without page/console errors. The generated asset report owns the catalog count and disposition. The build retains its journal-panel runtime-resolution and large Rapier chunk warnings.
- Local production-preview smoke evidence: HTTP 200, fresh `Enter Neva Land`, 1440×900 canvas, and no page/console errors after entry. This is local runtime evidence, not hosting or deployment evidence.

## Guidance update — 2026-08-26

The canonical ground guidance now distinguishes selectively smoothed traversable grass/soil/path surfaces from strongly faceted cliffs, cuts, exposed banks, rocks, and hero landforms. It also requires terrain materials, authored routes/road presentation, shoreline dressing, and ground-cover density to derive from the same world-layout semantics.

The guidance is now implemented in the working tree. `WORLD_LAYOUT_V5` remains the implementation symbol for the finite authored terrain field, with no chunk-streaming system. Current revisions and physical-road/village migration history belong to `01` §6.1. The following rendering observations are historical; the starter mountain evidence above owns this task’s verification. `terrainBaseHeight()` owns the graded landform, while final `terrainHeight()` adds deterministic nonnegative road crown/rut/shoulder/feather relief and remains the placement/anchor/normal/save-grounding authority. Three.js renders indexed road geometry at that exact final height; Rapier combines `terrainBaseHeightfield()` with a static trimesh from the same road geometry, while catalog bridge collision continues to own the bridge deck. Terrain uses smooth indexed normals/colors on continuous ground and blends toward face normals/colors for steep or cliff-heavy samples; `TerrainSurfaceMaterial` is non-flat with the r174-v3 cache contract. After the first integrated human review rejected opaque olive road bands and evenly scattered dark grass, the unchanged shared road mesh now alpha-feathers its vertex-colored shoulder into the canonical terrain and uses softer rut contrast. Existing grass GLBs now form stronger deterministic density pockets with variant-specific broad, lower silhouettes, retained semantic exclusions, no grass shadow-map casting or receiving, and centralized low/medium/high density scales of 0.24/0.48/0.60.

The original physical-road change has save impact **yes** and migration **yes**. The subsequent 2026-08-26 human-review correction changes only road blending, rut color contrast, and ground-cover presentation: save impact **no**, migration **no**. The later authored station, walkable-topology, and coast-topology changes have save impact **yes** and migration **yes**. The current schema/layout versions and per-version history are owned by `01` §6.1. The repository fixtures cover the historical topology migrations, while focused migration tests cover v19 → v20 rod ownership, the v20 → v21 Work-pool rescale, the v21 → v22 fish-quality/knowledge backfill, and the v22 → v23 market-state rebase. The live catalog/public manifest and corresponding public GLBs are synchronized; the generated asset-budget report owns the current counts. The 2026-08-27 human visual-gold decision predates the coast topology pass and must be re-reviewed for the affected harbor/coast scenes. Technical-art certification and the current-tree benchmark gate remain open.

Catalog `repo://` isolated studio sheets under `tools/blender/references/isolated/` are present on disk and git-tracked. Numbered crop PNGs listed in `tools/blender/references/README.md` (`01_hero_farmhouse.png` and siblings) were never checked in; treat that as graphics-extract debt, not a second reference authority.

## Historical implementation update — 2026-08-27

The asset and quest audit described here was implemented in the working tree at that date. The 2026-08-30 update above supersedes its pre-change counts, persistence status, Work rules, rod gate, and P12 status.

### Current asset baseline

- The live source catalog and public manifest now contain **189 assets**. Report provenance, counts, and `artContractPassed` are owned by `generated/reports/asset_budget_report.json` (last read: `generatedAt 2026-08-30T10:06:28.447Z`).
- Report summary at that read: **158 on target**, **31 below target**; the 2026-08-27 four-scene human visual-gold decision is recorded, while technical-art certification and the current-tree benchmark rerun remain open.
- Authored world placement now includes the previously unplaced `prop_wagon_cart_a` and `fauna_cow_a`.
- `fish_trout_a` and `fish_tuna_a` are now presented as animated school members when their corresponding sport-fish school is active.
- Tomato/potato stages remain conditional crop presentation. `boat_skiff_a` is a progression-world asset: it is not created by a fresh save, but is now materialized by the live skiff purchase contract after the Fishing XP and money requirements are met.

### Complete asset coverage contract

- `src/render/assets/AssetCoverage.ts` derives a one-record-per-catalog-asset contract from the catalog, the authored environment layout, runtime landmark owners, NPC/crop/fish/boat definitions, and action attachment bindings. It does not maintain a second filename registry. The live catalog and public manifest each contain 189 records, with publication identity synchronized through the owning art workflow.
- Current disposition counts are **90 static-world**, **15 dynamic-world**, **18 conditional-world**, and **1 progression-world**; **144** assets are visible in a fresh save. Every record includes its placement source, world context, fresh-save visibility, and activation trigger.
- `tests/unit/assetPlacementAudit.test.ts` fails on catalog/public manifest mismatch, duplicate or unclassified IDs, missing GLBs, invalid grounding, unusable boat moorings, or missing crop/fish/tool bindings. Current record, GLB, coverage, generated/public byte-parity, and synchronized publication identity checks cover 189 assets.
- Static farm, village, bridge, harbor, and coast landmarks are loaded by `WorldScene`/`WorldEnvironmentLayout`; crop stages are reachable through planting and growth; trout/tuna meshes are reachable through active sport-fishing schools; action tools/props are reachable through their simulation-owned presentation sockets; the skiff is reachable at `HARBOR_SKIFF_MOORING` after purchase.

### Current quest and persistence implementation

- The story chain links every quest with an explicit `nextQuestId` rather than relying on registry insertion order (`ContentRegistry` owns the roster and its size).
- Objective events now require exact targets and declared farm/station/habitat/market/boat context. The Act 2 compost step, Act 5 chum step, dock step, and final Silas report are explicit objectives.
- NPC dialogue and quest completion now require speaker proximity, the active quest, a completed final step, and atomic reward capacity. Rowboat commissioning consumes **30 G + 1 Ground Grain** and records `boat.player_rowboat` in persisted quest feature unlocks.
- Fresh saves retain the physical docked rowboat as world content, but boarding is locked until the commission reward is granted. Schema **11** preserves the existing keyed `boats` record; no schema bump is required for the persisted second skiff. The current migrations preserve the feature-unlock list, the one-time Act 5 starter-school flag, harbor fish-table structure, and legacy cargo conversion.
- Act 5 guarantees one deterministic lake trout school after the rowboat is commissioned, independent of fresh-save weather/timing. Cargo emits `FishLanded` before `CargoLoaded`, allowing the authored land → stow progression to advance.
- The skiff purchase is atomic: it validates the harbor access point, Fishing XP ≥ 15,000, and 850 G before creating `boat.player_skiff`, its supply inventory, cargo slots, mooring position, and persisted ownership. Boarding, docking, cargo, HUD, ledger, physics, and rendering resolve the active boat by ID.
- The authored farm and harbor layouts keep interaction footprints readable: farm production props occupy clear meadow/road footprints; the harbor market is inland at `HARBOR_MARKET` `(64, 60)`, the persisted fish-cleaning table is at `(70.8, 61.8)`, Maeve uses the market-side approach `(65.5, 66.9)`, Silas uses the harbor-pier approach `(83, 61)`, and the docked rowboat, crates, barrel, trap, net rack, lamps, and skiff mooring use separate deterministic anchors. The harbor arterial and coastal walk terminate at the same market anchor; schema 15/layout 7 migrates the persisted fish table and other canonical stations to the current composition, schema 16 keeps those world anchors unchanged, and schema 17/layout 8 re-grounds them against the revised coast profile without moving X/Z.
- The quest tracker is mounted in the HUD, the journal contains active/completed quest records, dialogue reward UI snapshots the completed quest, planner access is gated by `feature.expedition_planner`, and quest changes are autosave triggers.

### Current narrative contract — 2026-08-27

- The current story is a ten-quest, linear coastal-inheritance spine: arrival/welcome → stewardship → farm-to-fishing preparation → river learning → village exchange → harbor responsibility → earned rowboat → first expedition → open horizons. Exact text and objective data remain in `src/content/quests.ts`; NPC roles and idle texture remain in `src/content/npcs.ts`.
- Narrative is integrated with the loop through explicit people, places, actions, and consequences. Elspeth owns the welcome/soil/trade handoffs; Barnaby connects harvest to compost, grain, and chum; Old Silas teaches currents, controls the family rowboat threshold, and receives the final report; Maeve teaches freshness, demand, and responsible sale.
- The simulation owns the story outcome: `QuestDomain` validates proximity, speaker, target/location predicates, final-step completion, atomic rewards, and `nextQuestId`. The dialogue modal, HUD, journal, audio, and world art are presentation layers and cannot advance lore independently.
- Current live presentation is contextual intro/completion/idle dialogue, compact quest tracking, and active/completed quest titles in the journal. Dialogue page position is transient; `unlockedDialogueIds`, branching dialogue, persistent transcripts, and a separate lore-codex state are not live.
- This narrative is implemented and simulation-tested, but it is not yet browser-certified as a continuous experience. The P12 browser gate must verify the actual dialogue overlay at each handoff, close/reopen behavior, completion/reward presentation, and durable quest history after reload.

### Current quest coverage map

| Quest area | Current state |
|---|---|
| Act 1 farming | Elspeth, starter farm, wheat objectives, farm-scoped events, and mounted HUD tracker are implemented. |
| Act 2 processing | Compost, windmill/hand-mill, and workbench objectives are explicit and station-scoped; waypoints use canonical anchors. |
| Act 3 river/market | River habitat and village-market predicates are enforced; bridge and produce-stall waypoints use canonical anchors. |
| Act 4 harbor | Maeve and Silas require proximity; rowboat commission requirements and feature unlock are enforced. |
| Act 5 expedition | Board → chum → hook → land → stow → dock → sell → report is explicit; deterministic starter school and fish presentation are implemented. |

### Current verification boundary

- The human-review correction for road blending and grass presentation passes **62/62** focused terrain, road-geometry, road-environment, physics, and terrain-material tests. Typecheck, lint, codegen parity, and the production build pass. The Playwright CLI wrapper could not resolve its `playwright-cli` binary; the direct-library browser evidence for the P12 work is recorded below and is separate from the corrective art pass.
- The 2026-08-26 terrain/road focused suites are recorded as green, including **74/74** current world-layout/road/physics/persistence tests and **11/11** road/material tests. The prior **364/364** Vitest result and the later in-flight **48-file / 372-test** result are historical snapshots, not current full-suite proof; do not promote either to a new phase claim without rerunning the affected suite against this tree.
- The 2026-08-27 direct `tsc --noEmit`, lint, and production build evidence predates the current 189-asset snapshot. Lint then reported one pre-existing `console` warning in the gameplay-systems skill template; the build also reported a missing journal panel reference and the known large Rapier chunk. Current validation claims must come from current-tree checks.
- `npm run art:test-builders` currently fails before the Python builder suite starts: Blender 5.2.0 LTS exits with SIGSEGV during Metal backend initialization. Earlier semantic determinism evidence exists for 10 living-world assets (`foliage_flower_drift_a/b/c`, `foliage_meadow_tall_a/b`, `prop_path_slab_a/b`, `fauna_gull_a`, `fauna_butterfly_a`, `fauna_rabbit_a`), but full-family clean-source determinism and selected-publication identity hashes remain open.
- A separate 0.60-density high-tier debug view measured **103 draw calls and 887,487 visible triangles**. The fresh standard P0.75 benchmark recorded zero browser errors but, on the intentionally unmerged DEV path, measured farm **723 / 1,238,709**, bridge **9 / 185,286**, harbor **9 / 185,286**, and coast **261 / 911,948** draws/triangles. The representative debug view and the unbatched DEV benchmark are diagnostic evidence, not interchangeable production-equivalent proof.
- Chromium local review matrix now boots farm, crop stages, farmhouse interior, village market, bridge, harbor/rowboat/skiff, coast, trout, and tuna views with a canvas, coverage diagnostics, zero console errors/warnings, and no repeated `toNonIndexed()` warnings. The earlier focused matrix remains mechanical evidence only; the current isolated P0.75 benchmark exposes DEV static-prefab budget failures documented in the roadmap table. Human integrated gameplay-camera approval is recorded only for the four visual-gold scenes; full cross-browser coverage and complete quest-flow E2E proof remain outstanding.
- The runtime loader preserves authored full-resolution visibility when the current published GLB is missing an optional catalog LOD node; strict art validation remains responsible for rejecting that catalog/artifact mismatch before publication.
- Static/type/test evidence does not replace integrated gameplay-camera review, complete cross-browser gates, or human visual approval.

### P12 evidence delta — 2026-08-26

- `tests/simulation/p12VerticalSlice.test.ts` passes as a new-save command-boundary loop: Elspeth → wheat plant/water → durable save/offline growth/reload → harvest → compost/worms → Barnaby → mill → chum → river fish → village sale → Maeve → Silas rowboat commission → board/sail → deterministic trout school → chum/hook/land → physical cargo/freshness → dock/sell → final Silas report → durable reload. It asserts the completed quest chain and persisted cargo/boat state.
- Direct Playwright library fallback (the repository wrapper cannot resolve `playwright-cli`) reaches the real title-start flow in Chromium, Firefox, and WebKit on the historical 172-asset snapshot, with the 08:00 HUD clock. The same browser pass verifies keyboard movement plus inventory in all three engines; Chromium additionally verifies plant placement, rowboat board/drive/exit, and sport-fishing reel/slack/brace controls. Screenshots are in `output/playwright/p12-browser/`.
- A subsequent browser attempt against the then in-flight catalog was stopped at boot when `house_farmhouse_b.glb` was requested but absent; that historical source snapshot had 179 entries while the public manifest had 172. No invalid partial browser result is being counted as P12 evidence.
- This is stronger current evidence, but it is not a claim that the complete new-save quest loop has been executed through browser walking/sailing and every UI transaction. P12 remains open until that continuous browser matrix and its save/reload proof are completed.

## Ground supporting-map update — 2026-08-28

The renderer now consumes processed Poly Haven CC0 supporting maps as the Art Bible's optional low-frequency tiler. Grass Path 2 feeds the shared road mesh; Leafy Grass and Sparse Grass feed meadow-masked terrain. Provenance and URLs live in `src/render/materials/ExternalSurfaceTextures.ts`; sampling and blend strengths live in `VisualRenderConfig`; published WebPs live under `public/assets/textures/terrain/`. Materials remap into `PaletteTokens`; photographic RGB is not the final diffuse. Canonical height, route geometry, Rapier, placement, and save schema are unchanged.

`Save-impact: no`. `Migration required: no`. Mechanical presence is not visual approval. **Awaiting human game review** at gameplay-camera distance.

The P0.5 row below records that this presentation path exists. Do not promote it to a new gold-slice or release claim.

## Lighting, atmosphere and canopy-wind pass — 2026-09-04

Renderer-baseline change across the whole day cycle. `VisualRenderConfig` owns
every number; `LLM/04_ART_DIRECTION_BIBLE_PREMIUM_COZY_LOW_POLY.md` sections 7,
7.1, 9 and 16 own the rules. Five changes:

- **Golden hour.** The key's daylight ramp reached zero just below the horizon, so
  the sun switched off exactly when it was lowest and warmest and dawn/dusk were
  a flat ambient lift. The ramp now carries the key across the horizon
  (`sun.daylightFullSolarHeight` / `daylightZeroSolarHeight`) and one
  horizon-proximity envelope (`twilight.solarWidth`) drives both the warm sky band
  and the key colour, which is anchored on the day key so noon is unchanged. New
  `horizon_gold_01` sky token.
- **Shadow range.** `shadows.intensity` is no longer fully opaque, which was
  crushing dark palette families to flat black. `shadows.nightIntensity` /
  `nightRadius` give the moon its own lighter, broader recipe.
- **Night key-to-fill.** Night ran fill-dominant (moon 0.92 : fill 0.62) against
  day's 3:1, so moonlit forms had no lit side. Now key-dominant at roughly
  constant total night brightness.
- **Emissive response.** `bloom` and `grade` were declared and read by nothing.
  Emissive palette materials now scale with the practical-light envelope, so lit
  windows fade up at dusk instead of glowing at noon, and practical lights carry
  additive glow sprites capped by `practicalLightBudget` rather than a fullscreen
  bloom pass the low tier cannot afford.
- **Canopy wind.** Trees were the only static thing in a moving frame. Sway is a
  vertex offset inside the existing shared vegetation variant material.

Village dwellings (`house_cottage_a/b/c`, `building_inn_a/b`) gained a
`*_lantern_glow` node through the existing `preserve_names` mechanism, reusing the
palette emissive they already carried so no asset gained a material; four lamp
posts were added along village and orchard routes. Market halls, barns, sheds and
outhouses are deliberately excluded — see `VILLAGE_LANTERN_VARIANTS`. Of those
dwellings only `house_cottage_a` still ships; the `_b`/`_c` cottages and both inn
variants were retired when the authored village buildings replaced them.

Evidence: `tests/unit/lightingRig.test.ts`, `rendererFoundation.test.ts`,
`practicalLights.test.ts` and the new `tests/unit/vegetationWind.test.ts` pass;
`npm run typecheck` and `eslint` on the touched files are clean; the five
regenerated GLBs publish with their glow nodes and hash-match the manifest.

Performance was measured, not assumed. `npm run test:budget` reports 588 draw
calls / 3,044,983 triangles with this pass enabled and 595 / 3,048,971 with its
render additions disabled — identical within frame-sampling noise, so this change
is draw-call neutral. That gate is nonetheless **failing against a 220-draw /
900k-triangle high-tier budget for reasons that predate this pass**; it is not
evidence for or against this change beyond the neutrality above.

`Save-impact: no`. `Migration required: no`. Mechanical presence is not visual
approval, and the four approved daylight gold baselines have **not** been
re-approved. **Awaiting human game review** at gameplay-camera distance.

## Coast three-zone texture fix — 2026-08-31

Coast transitions no longer use a binary `terrainGreenMask` cutoff. The shader derives `vegetationMask` and `shoreMask` from packed surface weights with a gradual crossfade; shore receives meadow polygon breakup plus road-style sand meso/fine flecks. Baked `terrainGreenMask` is proportional for debug. Shader cache key `neva-terrain-surface-r174-v22`; beach sample scales `6 m` / `14 m`.

`Save-impact: no`. `Migration required: no`. Focused verification: `npx vitest run tests/unit/coastTextureMask.test.ts tests/unit/terrainSurfaceMaterial.test.ts`. **Awaiting human game review** at gameplay-camera distance.

## Compatibility-first shared surface field update — 2026-08-28

The connected starter-farm → road → bridge/riverbank slice now uses one presentation-only `WorldLayout.terrainSurfaceSample(x, z, sampledNormalY?)` contract. The existing `terrainSurfaceWeights()` API delegates to the same normalized weights, preserving its equations. The sample also exposes `farmInfluence` and `shorelineWetness`; it does not modify `terrainHeight()`, `roadSurfaceSample()`, route profiles, collision, water, anchors, or serialized state.

`SurfaceFieldAttributes` packs the shared field onto terrain, final post-conform road geometry, and the cultivated starter-farm bed: `surfaceWeights0` (grass/meadow/dry-soil/damp-soil), `surfaceWeights1` (path/shoulder/beach/riverbed), and `surfaceCauses` (wet shoreline/cliff/farm influence/shoreline wetness). Existing terrain attributes remain available as compatibility aliases. `SurfaceFieldShader` centralizes the world-XZ polygon signal, palette weighting, wetness, roughness, and filtered facet-normal stages. `TerrainSurfaceMaterial` and `RoadSurfaceMaterial` retain their public APIs; `CultivatedSurfaceMaterial` reuses the soil palette and existing farm vertex-color variation.

`WorldEnvironmentLayout` now consumes the shared meadow/path/farm/wet-shore meanings for equivalent ground-cover predicates while retaining exact path-specific predicates, seeds, candidate order, stable IDs, counts, asset categories, and authored `coastProfile` behavior. `VisualRenderConfig.groundSurface` owns the shared macro scale (`1.2 m`), the existing road core breakup remains `0.75 m`, and the shared wetness timing remains 3 seconds rising / 8 seconds falling.

`Save-impact: no`. `Migration required: no`. No new assets, dependencies, routes, topology, or gameplay state were added.

Focused verification: the shared-field/material/environment Vitest run passes **22/22** tests; the targeted post-conform terrain/road geometry checks pass **3/3**; direct `tsc --noEmit`, changed-file ESLint, and direct Vite production build pass. The `npm run build` wrapper remains blocked by unrelated unused declarations in `tests/unit/empirical_m4_challenger_secondary_sockets.test.ts`. The isolated `art:benchmark` rerun found no shader/page errors but exceeded the preferred farm budget at **1,571 draws / 2,439,681 triangles**; its later bridge/harbor/coast captures did not reach their intended views. Mechanical evidence is not visual approval; **Awaiting human game review** from the actual gameplay cameras.

## 2. Git and verification snapshot

### Git

> Regenerate this block with `git` before citing it. A stale git snapshot is
> worse than none; do not carry old hashes forward.

- Branch: `main`
- HEAD: `643d35b4279e7b0be7344d8224aa1624b02522e5` (`fix(physics): apply bridge-aware leading edge height to grounded player traversal`, 2026-08-30 12:29:31 +0300)
- Repository history: 34 commits.
- Working tree at this snapshot: 209 changed paths — 138 tracked modifications and 71 untracked paths.
- This snapshot therefore describes `main` **plus a large uncommitted working tree**, not a clean commit or release candidate.
- No save migration or deployment was performed by this audit.

### Checks run for this audit

The table below is retained only as the historical pre-change gate snapshot. Use the current implementation evidence in section 1 above for this terrain/road pass; do not reinterpret the older counts as current results.

| Check | Result | What it proves |
|---|---|---|
| `npm run art:codegen:check` | Historical pass; 92 catalog assets | The generated TypeScript projection matched the then-current catalog; this row is not current 189-asset evidence. |
| TypeScript `tsc --noEmit` | Pass | Current TypeScript sources type-check. |
| `eslint .` | Pass with 3 `console.log` warnings in `neva-world-check.ts` | No lint errors; warnings remain. |
| `npm run build` | Pass | Production bundle builds. Output was 14.79 MiB, under the documented 20 MiB initial-load target. Vite warned that the Rapier chunk is about 2.06 MB minified. |
| Full Vitest suite | **~262 `it`/`test` cases in `tests/` (grep 2026-08-25); typecheck expected green** | Do not use the old 212-case fail snapshot. Suite is larger now. Passing count last reported around 255; this checklist does not re-run the suite. |
| Chromium gameplay/control E2E | **Art review matrix passes; full gameplay loop remains unproven** | The 11-view authored placement matrix boots cleanly, but do not mark P12 complete without the new-save quest/control loop. Do not cascade this into empty/Red for P0–P11 systems that exist in src. |
| Firefox continuation | Incomplete | The first movement/camera scenario passed; the deep jump case was skipped by design; the run was stopped after repeated Chromium blockers. |
| WebKit | Not run | No current Safari/WebKit evidence. |
| Browser screenshot inspection | Mechanical inspection only | The game rendered a farm scene, character, HUD, controls, and debug overlay. This is not visual approval. |

### Current automated failures

Historical (stale) Vitest failure clusters — do not treat as current:

- `tests/simulation/gameplayFixes.test.ts` — 7 failures around habitat/catch expectations, fish-school coordinates, and the old safe-spawn position.
- `tests/simulation/persistence.test.ts` — the historical layout-revision expectation has been superseded by the passing schema-12/layout-4 fixture migration coverage.
- `tests/simulation/ownership.test.ts` — 2 crop-placement/ownership failures around the moved starter layout.
- `tests/simulation/simulationLoop.test.ts` — 1 fish-school/habitat failure, so the full deterministic vertical loop is not green.
- `tests/unit/physicsEdgeCases.test.ts` — 2 collision/tunneling failures.
- `tests/unit/physicsWorld.test.ts` — all 30 tests pass cleanly, including bridge traversal, boat collision, harbor pier stairs, and fish market approach (`npx vitest run tests/unit/physicsWorld.test.ts`).
- `tests/unit/worldLayout.test.ts` — all 35 tests pass cleanly, including regional route continuity, heightfield determinism, and harbor landmarks (`npx vitest run tests/unit/worldLayout.test.ts`).

Observed Chromium failures included insufficient movement after camera rotation, jump not returning to grounded within the assertion window, gait/presentation drift, harvest remaining in idle, farmhouse collision timeout, harbor/boat interaction instability, crop placement timeout, and deterministic sport-fishing setup failure. The isolated three-test gameplay smoke also failed to locate clock/debug elements even though its DOM snapshot and screenshot contained them; treat those three as a test-harness/served-build synchronization problem until diagnosed, not as proof that the visible HUD failed to render.

**These symptoms are now attributed, and the attribution is to the harness rather than to the game.** The 2026-09-06 browser-traversal work established that movement, stamina and progress checks in these specs were paced on wall-clock time while the application delivers movement on a capped fixed-step accumulator, so a loaded machine starves the actor and the assertion fails with nothing wrong in the runtime. The same investigation found nineteen defects in the P12 and control-foundation specs and none in gameplay code. Do not cite the list above as evidence about physics, traversal or the HUD; cite it as the symptom set that motivated the harness repair, and re-derive any gate status from a current run.

## 3. Roadmap gate status

Mechanical systems through P11 exist in src. P0.75 visual-gold acceptance is recorded; its separate technical-art certification remains open. P12 is human product-accepted, while automated browser save/reload and browser-matrix release proof remain open. The table separates code presence, product acceptance, and release evidence.

| Phase | Status | Current repository evidence | Missing or failing gate evidence |
|---|---|---|---|
| P0 — Repository & Architecture | **Partial** | Deterministic simulation structure, content registry, input/mode separation, Three.js presentation, debug HUD, schema/layout migration coverage and historical topology fixtures owned by `01` §6.1, synchronized catalog/GLB publication provenance, terrain-focused tests, lint, and focused browser input checks. | Release-wide browser and technical-art certification remain open. |
| P0.5 — Visual Rendering Foundation | **Partial** | `VisualRenderConfig`, palette materials/tokens, lighting, water, asset loader, generated catalog adapter, batching/instancing support, Art Yard, and diagnostics exist. The shared physical-road surface, base-heightfield + exact road trimesh, selective normals/colors, r174-v3 terrain material, and causal district/habitat/route/opening cover fields are implemented with focused tests. Processed CC0 supporting maps load through `ExternalSurfaceTextures` and remap into palette families; they are presentation-only. The additive frozen acceptance harness includes same-content no-post, field overlays, GPU timing, and render-target accounting. | Human gameplay-camera review remains pending for the changed river/vegetation composition; complete frozen software/hardware acceptance must be rerun before performance evidence is current. |
| P0.75 — Gold-Standard Art Slice | **Visual Gold Accepted / Technical Certification Open** | `approved-baselines.json` retains the four reference images and records the 2026-08-27 human visual-gold decision for bridge/farm/harbor/coast. The current published GLBs and manifest validate against the catalog; `generated/reports/asset_budget_report.json` owns the counts and budget disposition. The deterministic four-scene browser comparison is wired as a current-tree regression gate. | The 2026-08-31 renderer-baseline change (shadow caster policy and coverage, key/fill balance, clear-weather aerial perspective, terrain grid step, water shoreline) regenerated all four Chromium baselines, so every scene now differs from the images the 2026-08-27 decision was taken on and the human visual-gold decision must be re-taken. The current-tree benchmark, strict generation, and clean-source determinism also remain open technical-art/release gates. Pixel comparison is not human approval. |
| P1 — Walkable World | **Partial** | Large authored multi-district world with the northeast village as the market/mill/road hub, layout-9 asymmetric river/bed/floodplain topology, reserved fishing access, causal district boundaries, movement, mouse camera, interactions, collision adapters, and contextual prompts exist. | The new farm-to-bridge and river-to-harbor production movement captures plus human gameplay-camera composition review remain open. |
| P2 — Persistence & Time | **Partial** | IndexedDB repository, schema/layout versions owned by `01` §6.1, migrate-then-validate, **primary + backup keys only**. Historical topology fixtures remain; the per-version ledger is owned by `01` §6.1. Weather-bounded offline progression and quest `nextQuestId` remain intact. | Durable simulation coverage exists; full browser save/reload release proof is still open. Overlay pause is not a serializable sim mode. |
| P3 — Farming Vertical Slice | **Partial** | Placement/actions, moisture/fertility/growth/harvest (`farm.apply-fertilizer` +20, clamp 10–100), journals, starter plots, crop rendering, and the new-save plant/water/offline-growth/harvest simulation path pass. | Continuous browser plant-save-advance-load-harvest proof remains open. Orchard persist+regrow on successful harvest is LIVE; wither-delete of regrow crops is a parallel fix. |
| P4 — Inventory & Processing | **Partial** | Finite inventories, atomic operations, station-owned jobs, compost, the authored recipe set (`src/content/recipes.ts`; `content:validate` prints the live count), immutable pending-job snapshots, collection-time output capacity, equipment-result wardrobe reservations and the P12 compost/mill/chum command path pass. Schema-37 migration and focused full-inventory/cancel/reload coverage pass; one real Chromium Workbench flow starts, completes and collects Linen through the UI. Harbor fish-table remains at `HARBOR_FISH_TABLE`; fish-table recipes stay station-scoped. | The complete multi-station/multi-recipe browser loop, browser reload during each job phase, long-session balance and human crafting-comprehension review remain open. |
| P5 — Basic Fishing | **Partial** | Deterministic five-phase minigame and the P12 river-fish command path pass. A waiting full-satchel catch now remains pending with explicit Open satchel / Discard catch choices. | The catch-choice change is locally implemented but unverified; continuous new-save browser fishing proof remains open. |
| P6 — Boats | **Partial** | Rowboat/skiff definitions, atomic skiff purchase, boarding/driving/docking, two-boat persistence, Act 4 commission, and the P12 boat boundary pass. Chromium debug input proves board/drive/exit. | Emergency Tow / hook-class are not live. Continuous browser dock/board/sail and save/reload proof is not complete. |
| P7 — Sport Fishing | **Partial** | Deterministic schools, encounter state machine, held-state reel/slack/brace plus fish-left/right, species profiles, HUD, and the P12 landing path pass. | Human species-behavior identification and continuous new-save browser sport-fishing proof are not complete. |
| P8 — Physical Cargo & Freshness | **Partial** | Finite physical fish cargo, one-location modeling, freshness decay, transfer/sale logic, cargo UI, and P12 cargo/freshness/reload assertions pass. | Empty/partial/external-hook boat screenshots and a current browser transfer/no-clone proof were not completed. |
| P9 — Market Economy | **Partial** | Village, harbor, and Sunreach Cove markets use centered, deterministic supply demand with pure day/hour signals, exact marginal fills, a protected wholesale/retail spread, registry-derived retail wares, domain-owned market-board/quantity DTOs, atomic single and sell-all produce/fish transactions, gate-adjusted contract references, and executable balance coverage. A narrow Chromium village-market sale proved the live quote and committed purse update. Produce quality does not affect sale price (journal only). | Focused simulation/migration and one narrow browser transaction are current; continuous multi-market loop and human comprehension review remain open. |
| P10 — Progression & Contracts | **Partial** | Persisted rods and permanent slot equipment, ordered harbor purchase/equip, wardrobe reservations, clothing-only work-outfit presets, proficiency XP/ranks, quality-scaled harvest XP, the rounded integer Work-efficiency ceiling, representative Processing gate routes, seven contract templates with owned delivery markets, journal, gated expedition board, parallel linear `nextQuestId` tracks owned by `src/content/{quests,questTracks}.ts`, data-owned costs/rewards, validated objectives, and live skiff purchase. | Human reward-value judgment, broader balance and long-session progression/economy proof remain open. |
| P11 — Weather & Seasons | **Partial** | Seeded weather schedule, seasons, crop/market modifiers, renderer response, forecast/UI. Weather enum has **no drought**; growth buffs `light-rain` and `heavy-rain` at 1.05. | No current real-play proof that weather/season changes the correct player decision. |
| P12 — Full Vertical-Slice QA | **Human product gate accepted; automated route repaired and advancing** | The user completed a continuous manual playthrough through the authored quest ending on 2026-08-30; deterministic simulation coverage also exists. `tests/e2e/p12VerticalSlice.spec.ts` was found to be failing on its own staleness rather than on the game: nineteen harness defects were repaired on 2026-09-06 (see that record) and the route now drives a fresh save through planting, watering, harvesting, composting, two mill runs, chum, three bridge crossings, river fishing, the harbor, the village sale, boarding, sailing, chumming a school and landing a Rainbow Trout. No gameplay defect was found in that work. | The route has not yet completed end to end: the return sail and the closing market/epilogue legs are unproven, and two stalls remain unexplained by static analysis (a player position reported far from the leg being walked, and a boat halted on a fully sailable heading). Until a run completes, this row is repair progress, not a passing automated gate. The Chromium/Firefox/WebKit matrix remains separate P15/P16 work. |
| P13 — MVP Content Expansion | **Reached / Sunreach implemented locally** | `src/content/` owns every definition count; run `npm run content:validate` for the live totals. Restating them here drifted them by up to 4x before this line was replaced. Sunreach adds a second registry-driven terrain/coast/climate/ecology, skiff-gated sailing route, cove market, terrace farm, local processing and Act 7 loop. The focused Sunreach world/quest/persistence suites pass, both 64-seed composition audits pass, and the v25/layout-9 fixture preserves existing Neva truth. | Continuous real-input browser round trip, gameplay-camera comparison, human review, broader browser coverage, and balance proof remain open. |
| P14 — Final Art, Audio & UX Polish | **Reached / overhaul implemented, mechanically part-verified, human review open** | Catalog-driven art and audio remain intact. The coastal world-HUD/physical-interface overhaul is locally implemented across player-facing HUD, fishing/farming, title/recovery, dialogue, satchel, market, Hold & Stores, chart, journal/guide, expedition, pause/settings, confirmations, and landscape touch fallback; narrow simulation-owned HUD, market, crop/seed, forecast, sport-fight, satchel, logistics, chart, journal, pause, expedition, quest, and skill presentation queries plus atomic sell-all actions replace UI-owned derivation. 2026-09-03 mechanical evidence: typecheck pass; lint 0 errors / 116 warnings; `build` pass; `content:validate` pass (39 quests); UI unit batch 9 files / 103 tests pass; full Vitest 1133/1135 with 2 pre-existing fails (`gameplayFixes` rank-gated fertilizer XP, `artPipeline` manifest/toolchain drift); `gameplay.spec.ts` 6/6 Chromium pass; `control-foundation` 7/11 with the 4 historical movement/gait/placement blockers (since attributed to that spec's wall-clock pacing, not to gameplay code — see the 2026-09-06 traversal-harness record; the count predates the repair and is not a current result); screenshots in `output/playwright/{gameplay,start-screen,ui-audit}/`. E2E specs were realigned to the renamed P14 copy (`Satchel`, `Field Journal`, `Records`, `Sport fishing fight`, `Continue`, `Replace this harbor log?`, `Settings`, `Paused`, `Nautical Chart of Neva & Sunreach`, `Hold & Stores`). Two genuine P14 defects were fixed: the 1440 top-cluster overlap assertion is now mirror-aware of the intentional coastal side swap, narrow screens right-dock the play cluster plus fit the vitals tray to its container (`coastal.css` ≤820px block), and stacked-modal focus no longer snaps back to the HUD menu button mid-navigation (deferred restore plus opener stack in `useModalAccessibility.ts`). | Human in-game visual review, Firefox/WebKit smoke (config is Chromium-only), UI-scale 0.85×/1.25× extremes, long-text/keyboard/reduced-motion/touch audit, and audio/affected-scene approval remain open; no P14 completion claim. |
| P15 — Performance & Browsers | **Partial** | Build size is under the initial-load target; Meshopt, batching, camera-distance character detail LOD, instancing, and performance diagnostics exist. The fresh 11-view Chromium matrix stayed below 220 draws / 900k triangles, and direct fresh-title boot plus movement/inventory input passed in Chromium/Firefox/WebKit. | Complete cross-browser gameplay coverage and hardware-FPS proof remain open; the local review matrix is mechanical evidence only. |
| P16 — Release Candidate | **Not reached** | No valid completion claim. | Huge uncommitted tree, incomplete continuous P12 browser matrix, unresolved art validation/strict/determinism gates, exposed debug tools, and no full-loop release proof. |

### Roadmap completion checklist

- [ ] P0 gate passes from the current working tree.
- [ ] P0.5 renderer/material/Art Yard gate is re-proven from the current working tree.
- [x] P0.75 human visual-gold decision is accepted for the bridge/farm/harbor/coast gameplay-camera slices; `art:sync -- --all`, `art:validate -- --all`, and the 2026-08-27 registry decision provide the current human/publication evidence.
- [ ] P0.75 current-tree benchmark gate is green; the isolated run had no browser errors but exceeded the unchanged upper budgets because DEV static prefabs were not batched.
- [ ] P0.75 technical-art certification passes through strict generation and clean-source determinism; this remains a release/P16 gate.
- [ ] P1 walkable-world movement/camera/collision/input gate passes.
- [ ] P2 persistence/time release gate passes with the migration chain through the current schema/layout and all historical fixtures.
- [ ] P3 farming save/load/harvest-once gate passes.
- [ ] P4 inventory/processing/full-inventory/cancel-reload gate passes.
- [ ] P5 deterministic basic-fishing habitat gate passes.
- [ ] P6 dock/board/sail/dock/save/reload plus acquisition gate passes.
- [ ] P7 sport-fishing behavior and human identification gate passes.
- [ ] P8 physical cargo/freshness/no-clone gate passes.
- [ ] P9 market demand/freshness/single-formula gate passes.
- [ ] P10 three capability-changing milestones pass.
- [ ] P11 weather/season decision gate passes in real play.
- [x] P12 human product gate is accepted after the continuous manual quest-ending playthrough.
- [ ] P12/P15/P16 automated release proof passes the continuous save/reload flow and Chromium/Firefox/WebKit matrix.
- [ ] P13 content expansion receives complete runtime coverage and balance proof after the accepted P12 gate.
- [ ] P14 final art/audio/UX coverage passes current mechanical and human in-game review gates.
- [ ] P15 target performance and browser compatibility pass on representative hardware/browsers.
- [ ] P16 release-candidate gate passes from a clean, reviewable commit.

## 4. Vertical-slice acceptance checklist

The canonical list below comes from `02` section 19. Open boxes track missing automated or release evidence; they do not override the recorded human P12 product acceptance.

- [x] Move through starter world — current physics/world suites pass; direct Chromium/Firefox/WebKit movement input moved the player and returned to the inventory overlay.
- [ ] Sprint drains/recovers without affecting Work Capacity — unit mechanics exist; Chromium jump/sprint flow is red.
- [ ] Obtain wheat seed — a new save starts with wheat seed; an obtain/acquisition action is not proven.
- [ ] Plant and water wheat — the deterministic new-save simulation loop now passes both actions and Chromium placement input passes; the continuous browser flow remains open.
- [ ] Save/quit and return after offline growth — offline/persistence logic exists; not proven in the full current loop.
- [ ] Harvest exactly once — unit mechanics exist; current browser harvest/presentation flow fails.
- [ ] Place Worm Compost and harvest bait worms — the deterministic new-save simulation loop now passes; the continuous browser path remains open.
- [ ] Grind grain and craft chum — the deterministic new-save simulation loop now passes; the continuous browser path remains open.
- [ ] Basic fish — focused minigame and the deterministic P12 simulation path pass; browser basic-fishing transaction proof remains open.
- [ ] Commission rowboat — the deterministic new-save simulation path passes the 30 G + Ground Grain commission; browser boarding is proven in Chromium debug input but not through the continuous new-save quest.
- [ ] Sail to sport-fishing area — the deterministic P12 path passes the canonical boat movement boundary; continuous browser sailing remains open.
- [ ] Visually discover school — deterministic school activation and Chromium sport-fishing HUD input pass; integrated human/gameplay-camera proof remains open.
- [ ] Chum school — the deterministic P12 simulation path passes; continuous browser transaction proof remains open.
- [ ] Hook fish — the deterministic P12 simulation path passes; continuous browser transaction proof remains open.
- [ ] Reel/slack/brace — Chromium browser held-input assertions pass; continuous new-save browser encounter proof remains open.
- [ ] Land fish — the deterministic P12 simulation path passes; continuous browser landing proof remains open.
- [ ] Physically carry/store fish — cargo placement/freshness/reload pass in the deterministic P12 path; browser transfer/no-clone proof remains open.
- [ ] Freshness decays with time — deterministic P12 freshness assertion passes; browser save/reload proof remains open.
- [ ] Dock and sell — deterministic P12 dock/sell/report path passes; continuous browser dock/sale proof remains open.
- [ ] Price breakdown visible — UI implementation exists; no current passing browser gate.
- [ ] Gain Fishing XP — the deterministic P12 completion path advances progression; browser persistence/progression proof remains open.
- [ ] Buy/unlock capability — the deterministic rowboat commission capability step passes; browser milestone proof remains open.
- [ ] Reload with all progression intact — durable simulation save/reload passes; the continuous browser save/reload matrix remains open.
- [ ] Hear and understand the authored story spine — simulation dialogue payloads and quest chaining exist; browser proof of Elspeth → Barnaby → Silas → Maeve handoffs, completion lines, final report, and player comprehension remains open.

**Conclusion:** P12 is human product-accepted. The unchecked browser/save-reload items remain P15/P16 release-certification work and must not be represented as passed.

**On why these boxes stayed open.** Most of them cite "continuous browser proof remains open" while `tests/e2e/p12VerticalSlice.spec.ts` already existed to provide exactly that. The 2026-09-06 investigation found that spec had rotted against the P14 interface overhaul and against wall-clock timing assumptions, and was failing on itself: nineteen defects in the harness, none in gameplay code. Several of these boxes are therefore blocked on test repair rather than on missing game behaviour, and the repaired route has since exercised most of them in a browser without a gameplay failure. Do not tick a box from that observation — an advancing run is not a completed gate — but do not read the open boxes as evidence of missing or broken gameplay either.

## 5. Art and asset pipeline state

### Lean routine pipeline implementation

- [x] `art:preview` is absent from package scripts and CLI help paths; `tools/blender/preview.py` is deleted in the working tree.
- [x] Routine guidance says generate/publish, use the Art Yard, integrate in game, and await human game review without agent-led static preview scoring.
- [x] Catalog commands require an explicit asset/family or explicit release `--all` path.
- [x] Art Yard deep-link implementation and focused plugin tests exist.
- [x] Staging currently contains exactly three run directories.
- [x] The 189 generated/public asset copies and published manifests are synchronized to the current catalog/palette/toolchain provenance; no GLBs were regenerated or reauthored by this gameplay pass.
- [x] Normal generation keeps the published mechanical GLB contract intact and reports target debt separately.
- [x] `LLM/AGENTS.md` no longer says `BLENDER.md` covers previews; static previews are forbidden and `preview.py` is gone.

### Current asset acceptance debt

**`generated/reports/asset_budget_report.json` is the owner of this data. Do
not copy the below-target ID list into this file** — a hand-maintained copy
drifted to a wrong count and a wrong membership set before, and the report is
one command away:

```bash
node -e "const r=require('./generated/reports/asset_budget_report.json');console.log(r.summary);console.log(r.assets.filter(a=>a.qualityStatus==='below_target').map(a=>a.id).sort().join('\n'))"
```

Last read of that report (`generatedAt: 2026-08-30T10:06:28.447Z`):

- Assets: 189
- On target: 158
- Below target: 31 (`qualityStatus: "below_target"`; the report enumerates them)
- Hard contract failures: 0 (`artContractPassed: true`)
- Visual status: `candidate`, not human-approved by generation alone
- Strict report flag: `false`

The below-target set is churn-prone: it skews toward small props, items,
fauna, path slabs, tool sheds, and meadow foliage whose triangle targets exceed
what their silhouettes justify. Treat individual membership as report output,
never as a checked-in list.

Recorded human evidence: `tests/visual/reference/approved-baselines.json` retains the farm, bridge, harbor, and coast reference images approved by `human-art-director` on 2026-08-24 and records the 2026-08-27 human visual-gold decision for that four-scene scope without numeric scores. This decision is not approval of every individual asset and does not close technical-art certification; strict mechanical debt and clean-source determinism remain unresolved.

The 2026-08-31 renderer-baseline change supersedes the images that decision was taken on. `npm run visual:update` regenerated the four Chromium baselines and `npm run visual:test` re-passes against them, which is mechanical determinism evidence only; no human has reviewed the new gameplay-camera images.

## 6. Documentation/code drift to resolve

- [x] Implement the revised shared ground contract: class-aware/selective normals and colors, physical canonical road relief, exact shared render/Rapier road geometry, and clustered monotonic ground cover. Human gameplay-camera proof remains pending.
- [x] Update canonical save guidance through schema 23 / layout 8 while retaining historical topology migrations. `01` §6.1 is now the single migration ledger; `02` and `03` reference it instead of restating it.
- [x] Add `tests/fixtures/save_v14_layout6.json` and `tests/fixtures/save_v16_layout7.json` with coverage proving the station and coast topology migrations re-ground land truth without discarding unrelated simulation state; schema-16 normalization is recorded in the migration owner; a dedicated fallback assertion remains open.
- [x] Add `tests/fixtures/save_v11_layout3.json` and focused migration coverage for player, structures, active boats, and unrelated persisted state.
- [x] Preserve the physical-road upgrade as the schema-12/layout-4 migration, then reconcile the concurrent schema-13/layout-5 authority without dropping either fixture or migration.
- [x] Reconcile the canonical rowboat step: fresh saves retain the physical boat, while Act 4 commission now consumes 30 G + Ground Grain and unlocks boarding; browser proof remains open.
- [x] Reconcile the authored quest line with explicit event targets, location predicates, canonical waypoints, atomic rewards, cargo event ordering, and the deterministic Act 5 starter school.
- [x] Append the three-quest stewardship postscript with generic costs/rewards, strict content-chain validation, old-epilogue reconciliation, balanced expedition choices, milestone NPC recognition, and a visible journal knowledge entry; focused 7-file / 38-test evidence and one Chrome UI/autosave/reload route pass. First-time-player comprehension, continuous traversal, balance, and release acceptance remain open.
- [x] Reconcile the created-asset placement audit: wagon/cow are authored into the world and sport-fish GLBs are presentation-bound; conditional crops and the fresh-save-absent skiff are explicitly classified, with no audited asset left Art Yard-only.
- [x] Split P0.75 into the accepted four-scene visual-gold gate and the separate open technical-art/release certification gate; preserve current GLBs, target values, baseline images, and strict/determinism semantics.
- [x] Establish one canonical narrative contract across architecture, gameplay, roadmap, art direction, and art-production guidance; keep the live eighteen-quest linear spine separate from deferred branching/transcript/codex systems.
- [x] Reconcile P13's sequence with the accepted P12 product gate while preserving separate release evidence and not calling P13 complete from content-table presence alone.
- [x] Player-facing copy calls the mechanic **Work**. Full discounted Work is a hard atomic requirement for manual production; empty Work blocks without partial spending or reduced rewards.
- [x] Residual “previews” wording removed from `LLM/AGENTS.md`; remaining mention is that static previews are forbidden.

### Documentation audit — 2026-08-30

A full read of `LLM/` against the code, catalog, generated reports, and git
resolved the following drift. Every item below was verified against its owner,
not against another document.

- [x] Historical fish-quality/knowledge and centered-market migrations remain recorded in `01` §6.1 with their fixtures. The ledger owns current schema and layout versions; this checklist does not duplicate them.
- [x] `01` §6.1 created as the **single migration ledger**, in table form. It also recovers schema **v17**, which `01` had omitted entirely. `02` §18 and `03` §8 now reference it instead of restating the history in prose — that fourfold duplication is what caused the drift.
- [x] Below-target asset list removed from this file. The hand-copied list claimed 31 but enumerated 28 IDs, of which 6 were no longer below target and 9 current ones were missing. `generated/reports/asset_budget_report.json` is now cited as the owner, with the command to read it.
- [x] `LLM_AGENT_ART_PIPELINE_INSTRUCTIONS.md` said “33 below-target records”; corrected to cite the report rather than a number.
- [x] Recipe count corrected to **9** in `02` §5 and in the P4/P13 rows here. `recipe.carp_to_scraps` was live but undocumented.
- [x] Asset count corrected to **189** in the routing authority (it said 188; catalog and public manifest both hold 189).
- [x] `02` §11B added: mounts were a live `GameplayMode` with persisted state (schema v18) and no gameplay authority section. `MOUNT_TUNING` is named as the tuning owner and the dismount-before-work boundary is documented.
- [x] `02` §6 no longer implies a `Normal → Silver → Gold → Iridium` quality enum; those are UI atlas skin names. The canonical `FishQuality` ladder is restated inline.
- [x] `01`'s dangling “follow §41” corrected to §19 (the document ends at §19).
- [x] Git snapshot in section 2 regenerated. It had claimed a one-commit repository at a 2026-08-24 HEAD; the repository has 34 commits.
- [x] Duplicate routing authority resolved: repository-root `AGENTS.md` is now the single routing document, and `LLM/AGENTS.md` is a pointer retaining only the Three.js-skills routing table. The duplicate pair violated the projects' own “do not create parallel authority names” rule and had already diverged.
- [x] `06_AUDIO_AND_MUSIC_DESIGN_MASTER.md` registered in the canonical-authority list and in this file's audit basis; it was referenced by no canonical document. Its `Spec Ready` matrix is now explicitly labelled as “specified”, not “shipped”. `LAYOUT_EDITOR.md` likewise added to the authority list.
- [x] A **documentation-update contract** added to root `AGENTS.md`, with a fact-to-owner table, and propagated into the `01`/`02` Definitions of Done, the `03` task contract, save-sensitive protocol, and completion report, and the `BLENDER.md`/`LAYOUT_EDITOR.md` handoffs.

## 7. Next evidence-driven work order

These are the shortest blocking steps implied by the canonical phase order; they are not authorization to change gameplay direction.

1. [x] Advance the physical-road world through layout revision 4, reconcile the subsequent layout-5 northeast village hub, align shared height/collision owners, and cover both historical layouts through migration.
2. [ ] Fix the real physics/traversal failures: current focused corner/static/bridge/road suites pass and direct movement input passes in three engines; deep jump/sprint/camera/boat traversal proof remains open.
3. [x] Restore deterministic basic/sport fishing setup against the current habitats and world coordinates; the P12 simulation and Chromium held-input checks pass.
4. [ ] Diagnose Playwright's isolated locator mismatch using a fresh non-reused dev server, then rerun only the affected Chromium smoke/control tests. The wrapper is unavailable in this environment; direct Playwright library fallback passed the current browser checks.
5. [x] Make the deterministic P12 simulation loop pass from a new save, including the real rowboat acquisition/capability step; `tests/simulation/p12VerticalSlice.test.ts` passes.
6. [ ] Preserve the accepted P12 product decision while completing the continuous browser narrative/save-reload route as P15/P16 release evidence.
7. [ ] Rerun the complete browser matrix only after the narrow Chromium failures are green.
8. [ ] Continue the separate technical-art/release track with unchanged strict/determinism semantics. The below-target records in `generated/reports/asset_budget_report.json` remain visible technical debt and do not block the accepted four-scene visual lane.
9. [ ] Reconcile content/art/audio coverage for every authored crop and fish species during the reached but incomplete P13/P14 work. Take the roster from `ContentRegistry`, not from a number written here.
10. [ ] Profile representative hardware and Safari/WebKit for P15; do not treat SwiftShader FPS as hardware performance proof.
11. [ ] Split the large dirty tree into reviewable, intentional commits before any P16 claim.
12. [ ] Keep this checklist honest by construction: regenerate the git block and re-read
    `generated/reports/asset_budget_report.json` before citing either, and follow the
    documentation-update contract in root `AGENTS.md` on every change so status facts
    stop drifting between documents.

## 8. Completion definition for this checklist

When updating a box, attach the narrowest current evidence: command/test name, passing count, relevant asset IDs or simulation path, and whether it is static, browser-runtime, hardware-performance, or human visual proof. Never infer one proof layer from another.

<!-- RETAINED_EVIDENCE_END -->

</details>
